# Capítulo III - Requirements Specification

En esta sección, se presenta la especificación de requisitos de AgroSafe a partir de los hallazgos obtenidos en las etapas previas de análisis del problema, entrevistas, needfinding, Big Picture EventStorming y definición del Ubiquitous Language. Su propósito es traducir el entendimiento del dominio en un conjunto estructurado de requisitos que orienten de manera clara el desarrollo del producto dentro del alcance del proyecto.

A partir de ello, este capítulo organiza los requerimientos funcionales y técnicos de la solución en artefactos que permiten vincular necesidades de los usuarios, objetivos del producto y decisiones de implementación a nivel de MVP académico. En particular, se detallan las **User Stories**, el **Impact Mapping** y el **Product Backlog**, con el fin de asegurar trazabilidad entre el problema identificado, el valor esperado para los segmentos objetivo y el trabajo que deberá ejecutar el equipo durante el desarrollo del sistema.

## 3.1. User Stories

En esta seccion se presentan los requisitos funcionales y tecnicos del sistema AgroSafe IoT mediante la definicion de Epics, User Stories y Technical Stories. Las historias han sido elaboradas considerando los distintos roles que interactuan con el ecosistema (visitantes, agricultores, agronomos, personal interno y desarrolladores) y las capas arquitectonicas del sistema (Landing Page, Cloud API, Edge API, Embedded, Web App, Mobile App). Cada historia incluye criterios de aceptacion redactados en tercera persona y bajo la estructura Gherkin (Given-When-Then), garantizando que los requisitos sean claros, comprobables y agnosticos a la interfaz grafica. Adicionalmente, se incluyen *Technical Stories* orientadas a la implementacion de componentes internos del sistema como RESTful APIs, procesamiento en la nube, Edge computing e integracion con dispositivos IoT.

| Epic / Story ID | Titulo | Descripcion | Criterios de Aceptacion | Relacionado con (Epic ID) |
| :--- | :--- | :--- | :--- | :--- |
| **EP-001** | Digital Acquisition and Onboarding | Turn visitors into active users through a seamless sign-up and onboarding experience across Landing Page, Web App, and Mobile App. | N/A (Es un Epic) | EP-001-US001, EP-001-US005, EP-001-US002, EP-001-US003, EP-001-US004, EP-001-TS008 |
| **EP-001-US001** | Browse Landing Page Content | As a visitor, I want to browse the landing page content so that I can understand the product's value proposition and available plans before deciding to register. | **Scenario 1: View value proposition**<br>**Given** that the visitor accesses the home page of the static website<br>**When** the page loads completely<br>**Then** the system displays the product purpose, the key benefits, and a call-to-action element that redirects to the registration view.<br><br>**Scenario 2: View available plans**<br>**Given** that the visitor is on the home page<br>**When** the visitor navigates to the plans section<br>**Then** the system displays a comparison of available subscription plans with features, prices, and terms. | EP-001 |
| **EP-001-US005** | Browse Landing Page as Agronomist Visitor | As an agronomist visitor, I want to browse the landing page content tailored to agronomists so that I can understand the specific benefits for my consulting practice. | **Scenario 1: View agronomist section**<br>**Given** that the agronomist visitor accesses the landing page and selects the agronomist target option<br>**When** the visitor navigates to the agronomist section<br>**Then** the system displays the benefits for agricultural consultants, the multi-client management value proposition, and a registration form tailored to the agronomist segment. | EP-001 |
| **EP-001-US002** | Register as Lead | As a visitor, I want to register as a lead so that I can create an account and begin the onboarding process to use the platform. | **Scenario 1: Successful registration**<br>**Given** that the visitor fills out the registration form with valid full name, email, password, and role (farmer or agronomist)<br>**When** the form is submitted<br>**Then** the system creates the account, sends a verification email, and confirms that email verification is required to activate the account.<br><br>**Scenario 2: Registration with invalid data**<br>**Given** that the visitor enters incomplete or incorrectly formatted information<br>**When** the form submission is attempted<br>**Then** the system indicates which fields need to be corrected without losing the valid information already entered.<br><br>**Scenario 3: Duplicate email**<br>**Given** that the visitor submits a registration with an email that already exists in the system<br>**When** the registration is processed<br>**Then** the system rejects the registration and displays a message indicating that the email is already registered. | EP-001 |
| **EP-001-US003** | Verify Email Account | As a newly registered user, I want to verify my email account so that I can confirm my identity and activate access to the platform. | **Scenario 1: Successful verification**<br>**Given** that the system sends a valid verification link to the user's email<br>**When** the user accesses the verification link<br>**Then** the system confirms the verification, activates the account, and displays a confirmation message.<br><br>**Scenario 2: Expired verification link**<br>**Given** that the verification link has expired<br>**When** the user accesses the expired link<br>**Then** the system notifies the user of the expiration and offers the option to request a new verification email. | EP-001 |
| **EP-001-TS008** | Cloud RESTful API — Authentication and Registration Endpoints | As a Frontend Developer, I want the Cloud API to implement authentication and registration endpoints so that the Web and Mobile applications can integrate sign-up, sign-in, and email verification flows. | **Scenario 1: User signs up with valid data**<br>**Given** a POST request to `/api/v1/authentication/sign-up` is received with attributes: fullName (String), email (String), password (String), role (String: FARMER or AGRONOMIST)<br>**When** the API validates the request and creates the user<br>**Then** the API responds with 201 Created and returns the created user with attributes: id (Long), fullName (String), email (String), role (String), verified (Boolean).<br><br>**Scenario 2: User signs in with valid credentials**<br>**Given** a POST request to `/api/v1/authentication/sign-in` is received with attributes: email (String), password (String)<br>**When** the API validates the credentials successfully<br>**Then** the API responds with 200 OK and returns the authenticated user with attributes: id (Long), fullName (String), email (String), token (String), role (String).<br><br>**Scenario 3: Sign-in with unverified account**<br>**Given** a POST request to `/api/v1/authentication/sign-in` is received with valid credentials for an unverified account<br>**When** the API detects the account is not verified<br>**Then** the API responds with 403 Forbidden and returns an error payload indicating email verification is required.<br><br>**Scenario 4: Verification email is confirmed via token**<br>**Given** a GET request to `/api/v1/authentication/verify-account?token={tokenValue}` is received with a valid token<br>**When** the API validates the token<br>**Then** the API responds with 200 OK, marks the account as verified, and returns a confirmation message.<br><br>**Scenario 5: New verification email is requested**<br>**Given** a POST request to `/api/v1/authentication/resend-verification` is received with attribute: email (String)<br>**When** the API finds the unverified account<br>**Then** the API sends a new verification email and responds with 200 OK. | EP-001 |
| **EP-001-US004** | Complete Onboarding Wizard | As a new farmer user, I want to complete the onboarding wizard so that I can configure my farm data, irrigation zones, link IoT devices, and become operational from the first day. | **Scenario 1: Configure farm data and irrigation zones**<br>**Given** that the new user is in the initial setup flow<br>**When** the user enters farm data (name, location, hectares, crop type) and creates at least one irrigation zone<br>**Then** the system persists the farm and zone records, allows saving progress to continue later, and advances to the device linking step.<br><br>**Scenario 2: Finalize onboarding**<br>**Given** that the user has completed all setup steps (at least one farm and one zone)<br>**When** the user confirms the final onboarding step<br>**Then** the system marks the onboarding as complete, redirects to the main dashboard, and presents a guided tour of the available functionalities.<br><br>**Scenario 3: Incomplete onboarding**<br>**Given** that the user attempts to finalize onboarding without completing all required steps<br>**When** the system validates the completion requirements<br>**Then** the system prevents finalization and indicates which steps still need to be completed. | EP-001 |
| **EP-001-US004-FARM** | Cloud RESTful API — Farm and Zone Management Endpoints | As a Frontend Developer, I want the Cloud API to implement CRUD endpoints for farms, irrigation zones, and device-zone linking so that the Web and Mobile applications can build the onboarding wizard and farm management views. | **Scenario 1: Farm is created**<br>**Given** a POST request to `/api/v1/farms` is received with a valid Bearer token and attributes: name (String), location (String), hectares (Double), cropType (String)<br>**When** the API validates and creates the farm<br>**Then** the API responds with 201 Created and returns the farm with attributes: id (Long), name (String), location (String), hectares (Double), cropType (String), createdAt (String).<br><br>**Scenario 2: User's farms are listed**<br>**Given** a GET request to `/api/v1/farms` is received with a valid Bearer token<br>**When** the API retrieves the authenticated user's farms<br>**Then** the API responds with 200 OK and returns a list where each item contains: id (Long), name (String), location (String), hectares (Double), cropType (String), zoneCount (Integer), createdAt (String).<br><br>**Scenario 3: Farm is retrieved by ID**<br>**Given** a GET request to `/api/v1/farms/{farmId}` is received with a valid Bearer token for the farm owner<br>**When** the API finds the farm<br>**Then** the API responds with 200 OK and returns the farm with all current attributes.<br><br>**Scenario 4: Zone is created for a farm**<br>**Given** a POST request to `/api/v1/farms/{farmId}/zones` is received with a valid Bearer token and attributes: name (String), areaHectares (Double), cropType (String)<br>**When** the API validates and creates the zone<br>**Then** the API responds with 201 Created and returns the zone with attributes: id (Long), farmId (Long), name (String), areaHectares (Double), cropType (String), createdAt (String).<br><br>**Scenario 5: IoT device is linked to a zone**<br>**Given** a POST request to `/api/v1/zones/{zoneId}/devices` is received with a valid Bearer token for the zone owner and attribute: deviceId (Long)<br>**When** the API validates the link and associates the device<br>**Then** the API responds with 200 OK and returns the association with attributes: zoneId (Long), deviceId (Long), linkedAt (String). | EP-001 |
| **EP-002** | Smart Monitoring and Water Management | Centralize soil health monitoring and automate irrigation and fertigation to optimize water and nutrient resources through Cloud API, Edge API, Embedded, Web App, and Mobile App. | N/A (Es un Epic) | EP-002-US001, EP-002-US002, EP-002-US003, EP-002-US004, EP-002-US012, EP-002-US013, EP-002-US018, EP-002-US028, EP-002-US029, EP-002-TS005, EP-002-TS013, EP-002-TS015, EP-002-TS020, EP-002-TS006, EP-002-TS007, EP-002-TS008, EP-002-TS009, EP-002-TS026, EP-002-TS032, EP-002-TS033, EP-002-TS034 |
| **EP-002-US001** | View Real-Time Soil Dashboard | As a farmer, I want to view the real-time soil dashboard so that I can monitor moisture, EC, pH, and temperature of my parcels at any time and make informed irrigation decisions. | **Scenario 1: Real-time soil data is displayed for a zone**<br>**Given** that the farmer accesses the dashboard and selects a specific zone<br>**When** the system retrieves the latest soil sensor readings<br>**Then** the system displays the soil parameters (moisture, EC, pH, temperature) with current values, units, and the time of the last reading.<br><br>**Scenario 2: Critical status is highlighted**<br>**Given** that a soil parameter is out of the safe range defined for the crop type in that zone<br>**When** the dashboard retrieves and displays the data<br>**Then** the system highlights the affected parameter and displays an initial agronomic recommendation.<br><br>**Scenario 3: No recent sensor data is available**<br>**Given** that the farmer accesses the dashboard for a zone with no linked soil sensors<br>**When** the system queries for current soil data<br>**Then** the system displays an indicator that data is currently unavailable along with the timestamp of the last successful reading. | EP-002 |
| **EP-002-US002** | Receive Irrigation Alert | As a farmer, I want to receive irrigation alerts so that I am notified immediately when the soil reaches critical conditions and I can take timely action. | **Scenario 1: Alert is dispatched when critical soil condition is detected**<br>**Given** that a sensor reports a parameter value outside the configured safe range<br>**When** the system evaluates the agronomic rules against the sensor reading<br>**Then** the system dispatches a notification through the user's preferred channel, including the affected zone, the out-of-range parameter, and a recommended action. | EP-002 |
| **EP-002-US003** | Activate Irrigation Remotely | As a farmer, I want to activate irrigation remotely so that I can start watering my field from anywhere without being physically present on the farm. | **Scenario 1: Irrigation is activated successfully**<br>**Given** that the farmer selects a zone and triggers the irrigation activation with an optional duration in minutes<br>**When** the system validates the zone has a linked valve device and dispatches the command<br>**Then** the system confirms the start of the water flow and records the session in the activity log.<br><br>**Scenario 2: Activation fails because no valve device is linked**<br>**Given** that the farmer attempts to activate irrigation for a zone without a linked valve controller<br>**When** the system validates the zone configuration<br>**Then** the system prevents the activation and indicates that no valve device is linked.<br><br>**Scenario 3: Activation fails due to an already active session**<br>**Given** that the farmer attempts to activate irrigation for a zone with an already active session<br>**When** the system checks the session status<br>**Then** the system prevents the activation and displays the current active session details. | EP-002 |
| **EP-002-US004** | Stop Irrigation Manually | As a farmer, I want to stop irrigation manually so that I can interrupt watering early and avoid water waste when conditions change. | **Scenario 1: Irrigation is stopped successfully**<br>**Given** that the farmer triggers the stop irrigation action for a zone with an active session<br>**When** the system dispatches the stop command to the valve controller<br>**Then** the system confirms the halt of the water flow, finalizes the session record, and estimates the total water used and water saved.<br><br>**Scenario 2: Stop fails because no active irrigation exists**<br>**Given** that the farmer attempts to stop irrigation for a zone with no active session<br>**When** the system validates the session status<br>**Then** the system prevents the action and indicates there is no active irrigation to stop. | EP-002 |
| **EP-002-US012** | View EC and pH Trends | As a farmer, I want to view the EC and pH trends over time so that I can analyze soil chemistry evolution and detect salinity problems before they affect my crops. | **Scenario 1: EC and pH trend data is displayed for recent days**<br>**Given** that the farmer is viewing a zone's soil details and selects the trend view<br>**When** the system retrieves aggregated historical soil data for the specified period<br>**Then** the system displays the EC and pH evolution with reference lines indicating the optimal ranges for the crop type.<br><br>**Scenario 2: Alert is triggered when EC exceeds the critical limit**<br>**Given** that the EC parameter has exceeded the critical threshold during the viewed period<br>**When** the system renders the trend data<br>**Then** the system highlights the elevated EC periods and displays recommendations for correcting the salinity issue. | EP-002 |
| **EP-002-US013** | Receive High EC Alert | As a farmer, I want to receive a high EC alert so that I can act on soil salinity problems before they cause irreversible damage to my crops. | **Scenario 1: High EC alert is dispatched with recommendations**<br>**Given** that the EC sensor reading exceeds the critical threshold configured for the crop type<br>**When** the system evaluates the telemetry data<br>**Then** the system dispatches an alert detailing the current EC value, the recent trend, and agronomic recommendations in plain language.<br><br>**Scenario 2: Combined EC and low moisture alert is prioritized as urgent**<br>**Given** that EC is high and soil moisture is low simultaneously<br>**When** both conditions are met during evaluation<br>**Then** the system prioritizes the alert as Urgent and indicates that high salinity is blocking plant hydration. | EP-002 |
| **EP-002-US018** | Calibrate Sensor Thresholds per Crop Type | As a farmer, I want to calibrate sensor thresholds per crop type so that alerts and recommendations are adjusted to the specific needs of each crop I grow. | **Scenario 1: Recommended thresholds are auto-loaded when crop type is selected**<br>**Given** that the farmer configures an irrigation zone and selects a specific crop type<br>**When** the system retrieves the recommended thresholds for that crop<br>**Then** the system automatically populates the threshold fields and allows the farmer to adjust them manually.<br><br>**Scenario 2: Manual override with out-of-range value triggers a warning**<br>**Given** that the farmer enters a threshold value outside the safe agronomic range<br>**When** the farmer saves the configuration<br>**Then** the system issues a risk warning, requires explicit confirmation, and logs the exception in the audit trail. | EP-002 |
| **EP-002-TS005** | Cloud RESTful API — Telemetry Ingestion | As a Cloud Developer, I want the Cloud API to implement a telemetry ingestion endpoint so that sensor data from IoT devices is securely received, validated, and stored for consumption by dashboard and analytics features. | **Scenario 1: Bulk telemetry is ingested successfully**<br>**Given** a POST request to `/api/v1/telemetry/batch` is received with device authentication credentials and an array of readings: [{ deviceId (Long), metricType (String), value (Double), unit (String), timestamp (String) }]<br>**When** the API validates the data structure and persists the records<br>**Then** the API responds with 201 Created and returns a summary with attributes: ingested (Integer), failed (Integer), errors (List, nullable).<br><br>**Scenario 2: Telemetry values outside physically possible ranges are rejected**<br>**Given** a POST request to `/api/v1/telemetry/batch` is received with sensor values outside the physically possible range<br>**When** the API validates the payload<br>**Then** the API responds with 400 Bad Request and returns an error payload describing the out-of-range values. | EP-002 |
| **EP-002-TS013** | Cloud RESTful API — Parcels and Irrigation Zones CRUD | As a Frontend Developer, I want the Cloud API to implement CRUD endpoints for parcels and irrigation zones so that the Web and Mobile applications can manage zone configuration and crop-specific settings. | **Scenario 1: Parcel is created with nested zones**<br>**Given** a POST request to `/api/v1/farms` is received with a valid Bearer token and nested zones: [{ name (String), areaHectares (Double), cropType (String) }]<br>**When** the API processes the authenticated request<br>**Then** the API responds with 201 Created and returns the farm with nested zones, each with generated IDs and default crop thresholds.<br><br>**Scenario 2: Zone thresholds are updated**<br>**Given** a PATCH request to `/api/v1/zones/{zoneId}/thresholds` is received with a valid Bearer token and new threshold limits<br>**When** the API validates the values<br>**Then** the API responds with 200 OK, updates the thresholds, logs the change in the audit trail, and returns the updated state. | EP-002 |
| **EP-002-TS015** | Cloud RESTful API — Irrigation Command Endpoints | As a Frontend Developer, I want the Cloud API to implement irrigation command endpoints so that the Web and Mobile applications can send valve orders, schedule irrigation, and retrieve session history with full traceability. | **Scenario 1: Irrigation start command is queued**<br>**Given** a POST request to `/api/v1/zones/{zoneId}/irrigation/start` is received with a valid Bearer token and optional attribute: durationMinutes (Integer)<br>**When** the API validates no conflicting active session exists<br>**Then** the API queues the command for Edge delivery and responds with 200 OK returning the session with attributes: irrigationId (Long), zoneId (Long), status (String: PENDING), startedAt (String).<br><br>**Scenario 2: Irrigation is stopped manually**<br>**Given** a POST request to `/api/v1/zones/{zoneId}/irrigation/stop` is received with a valid Bearer token<br>**When** the API validates an active session exists and dispatches the stop command<br>**Then** the API responds with 200 OK returning the session with attributes: irrigationId (Long), status (String: STOPPED), totalWaterUsedLiters (Double).<br><br>**Scenario 3: Irrigation schedule is created**<br>**Given** a POST request to `/api/v1/zones/{zoneId}/irrigation/schedule` is received with attributes: startAt (String, ISO datetime), durationMinutes (Integer), recurrence (String: ONCE, DAILY, WEEKLY, CUSTOM)<br>**When** the API validates no time conflicts exist<br>**Then** the API responds with 201 Created and returns the schedule.<br><br>**Scenario 4: Irrigation history is retrieved**<br>**Given** a GET request to `/api/v1/zones/{zoneId}/irrigation/history` is received with a valid Bearer token and optional query parameters: from (String), to (String), limit (Integer)<br>**When** the API retrieves past sessions<br>**Then** the API responds with 200 OK and returns a list where each item contains: irrigationId (Long), status (String), startedAt (String), stoppedAt (String), waterUsedLiters (Double). | EP-002 |
| **EP-002-TS020** | Cloud RESTful API — Latest Telemetry Polling Endpoint | As a Frontend Developer, I want the Cloud API to expose a REST endpoint for the latest zone telemetry so that the Web and Mobile applications can retrieve current sensor readings by polling at a regular interval. | **Scenario 1: Latest telemetry is retrieved for a zone**<br>**Given** a GET request to `/api/v1/zones/{zoneId}/telemetry/latest` is received with a valid Bearer token<br>**When** the API retrieves the most recent sensor readings for the zone<br>**Then** the API responds with 200 OK returning a list where each item contains: metricType (String), value (Double), unit (String), timestamp (String), status (String).<br><br>**Scenario 2: Telemetry is filtered by metric type**<br>**Given** a GET request to `/api/v1/zones/{zoneId}/telemetry/latest?metricType={type}` is received with a valid Bearer token<br>**When** the API retrieves readings filtered by the specified metric<br>**Then** the API responds with 200 OK returning only readings for the requested metric type.<br><br>**Scenario 3: No telemetry available returns empty state**<br>**Given** a GET request to `/api/v1/zones/{zoneId}/telemetry/latest` is received for a zone with no linked sensors or no recent readings<br>**When** the API processes the request<br>**Then** the API responds with 200 OK returning an empty list with lastUpdated set to null. | EP-002 |
| **EP-002-TS006** | Edge API Processes Irrigation Valve Commands | As a Cloud Developer, I want the Edge API to receive valve commands from the Cloud and execute them reliably so that irrigation is executed accurately on the physical hardware through the microcontroller. | **Scenario 1: Edge relays a valve open command to the device**<br>**Given** that the Cloud dispatches an irrigation start command for a specific device via REST<br>**When** the Edge receives and validates the instruction<br>**Then** the Edge relays the command to the ESP32 via MQTT, the device activates the physical valve and starts the safety timer, and the Edge confirms execution back to the Cloud via REST.<br><br>**Scenario 2: Edge handles valve command failure**<br>**Given** that the Edge attempts to relay a valve command to the device via MQTT but the device does not respond<br>**When** the relay fails after configured retry attempts<br>**Then** the Edge reports the failure status back to the Cloud with the error code. | EP-002 |
| **EP-002-TS007** | Embedded ESP32 Reads Soil Sensors | As an Edge Developer, I want the ESP32 firmware to periodically read soil sensors and transmit data to the Edge so that a continuous and reliable flow of soil data is guaranteed from the field. | **Scenario 1: Firmware executes a periodic sensor reading cycle**<br>**Given** that the IoT device is powered on and has completed its boot sequence<br>**When** the programmed reading interval timer triggers<br>**Then** the firmware reads all connected soil sensors, packages the readings with device ID, metric types, values, units, and timestamp, transmits the payload to the Edge via MQTT, and returns the device to deep sleep mode.<br><br>**Scenario 2: Firmware handles sensor read failure gracefully**<br>**Given** that a hardware read error occurs on one sensor<br>**When** the firmware detects the failure<br>**Then** the firmware logs the error, retries the read once, and includes the sensor health degradation status in the next telemetry payload. | EP-002 |
| **EP-002-TS008** | Implement Biometric Authentication for Critical Valve Actions | As a Mobile Developer, I want the mobile application to require biometric authentication for irrigation valve control actions so that accidental activations and unauthorized operations are prevented. | **Scenario 1: Biometric authentication is required before valve activation**<br>**Given** that the farmer presses the valve activation button in the mobile application<br>**When** the application triggers the activation action<br>**Then** the application requests fingerprint or facial recognition and only proceeds to send the command to the Cloud API if biometric authentication is successful.<br><br>**Scenario 2: Biometric authentication fails and falls back to device PIN**<br>**Given** that biometric authentication fails the configured maximum number of attempts<br>**When** the retry limit is exhausted<br>**Then** the application falls back to the device PIN or password authentication and logs the security event. | EP-002 |
| **EP-002-TS009** | Embedded ESP32 Controls Irrigation Valve | As an Edge Developer, I want the ESP32 firmware to receive valve open and close commands from the Edge and execute them reliably so that irrigation is activated and stopped correctly on the physical hardware. | **Scenario 1: Firmware executes a valve open command**<br>**Given** that the ESP32 receives a validated valve open command with an optional duration parameter from the Edge via MQTT<br>**When** the firmware processes the command<br>**Then** the firmware activates the solenoid valve relay, starts the internal hardware timer for the specified duration, and confirms the state change back to the Edge via MQTT.<br><br>**Scenario 2: Auto-close safety fallback when timer expires**<br>**Given** that the solenoid valve is open and communication with the Edge is interrupted<br>**When** the internal hardware timer reaches zero<br>**Then** the firmware closes the valve automatically to prevent water waste, regardless of whether a stop command was received.<br><br>**Scenario 3: Firmware executes a valve close command**<br>**Given** that the ESP32 receives a validated valve close command from the Edge via MQTT while the valve is open<br>**When** the firmware processes the command<br>**Then** the firmware deactivates the solenoid valve relay, stops the hardware timer, and confirms the state change back to the Edge via MQTT. | EP-002 |
| **EP-002-TS026** | Implement Biometric Auth for Critical Actions (duplicate identifier) | (Consolidated with EP-002-TS008 — see above) | N/A (Consolidated) | EP-002 |
| **EP-002-US028** | Activate Fertigation Remotely | As a farmer, I want to activate fertigation (the injection of liquid fertilizer into the irrigation line through a dedicated electrovalve) remotely so that I can deliver nutrients to my crops precisely and on time without being physically present on the farm. | **Scenario 1: Fertigation is activated successfully**<br>**Given** that the farmer selects a zone that has a linked fertigation electrovalve and a dosing configuration (fertilizer type, injection ratio, optional duration) and an irrigation session is already active for that zone<br>**When** the system validates the configuration and dispatches the fertigation command<br>**Then** the system opens the fertigation electrovalve, starts the dosing pump, begins nutrient injection, and records the fertigation session in the activity log.<br><br>**Scenario 2: Fertigation is blocked when no irrigation is active**<br>**Given** that the farmer attempts to activate fertigation for a zone with no active irrigation session (no water flow)<br>**When** the system validates the irrigation state<br>**Then** the system prevents the activation and indicates that irrigation water flow must be active before injecting fertilizer to avoid root burn and undissolved product.<br><br>**Scenario 3: Fertigation is blocked when no fertigation device is linked**<br>**Given** that the farmer attempts to activate fertigation for a zone without a linked fertigation electrovalve<br>**When** the system validates the zone configuration<br>**Then** the system prevents the activation and indicates that no fertigation device is linked to the zone. | EP-002 |
| **EP-002-US029** | Configure Fertigation Recipe and Schedule | As a farmer, I want to configure fertigation recipes and schedules per crop type so that nutrient delivery is automated and adjusted to each crop's growth stage and target soil chemistry. | **Scenario 1: Fertigation recipe is created**<br>**Given** that the farmer defines a recipe with fertilizer type, target EC, injection ratio, and default duration for a zone<br>**When** the farmer saves the recipe<br>**Then** the system persists the recipe associated with the zone and its crop type for reuse in manual and scheduled fertigation.<br><br>**Scenario 2: Fertigation schedule is created without conflicts**<br>**Given** that the farmer schedules a fertigation event with a start time, duration, and recurrence (ONCE, DAILY, WEEKLY, CUSTOM)<br>**When** the system validates that the fertigation schedule overlaps with an active irrigation window and does not conflict with another fertigation schedule<br>**Then** the system persists the schedule and confirms it is active.<br><br>**Scenario 3: Recipe with unsafe target EC triggers a warning**<br>**Given** that the farmer sets a target EC above the safe agronomic range for the selected crop type<br>**When** the farmer saves the recipe<br>**Then** the system issues a salinity-risk warning, requires explicit confirmation, and logs the exception in the audit trail. | EP-002 |
| **EP-002-TS032** | Cloud RESTful API — Fertigation Command Endpoints | As a Frontend Developer, I want the Cloud API to implement fertigation command endpoints so that the Web and Mobile applications can start and stop nutrient injection, configure recipes, and retrieve fertigation history with full traceability. | **Scenario 1: Fertigation start command is queued**<br>**Given** a POST request to `/api/v1/zones/{zoneId}/fertigation/start` is received with a valid Bearer token and attributes: fertilizerType (String), injectionRatio (Double), durationMinutes (Integer, optional)<br>**When** the API validates that an active irrigation session exists for the zone and no conflicting fertigation session is running<br>**Then** the API queues the command for Edge delivery and responds with 200 OK returning the session with attributes: fertigationId (Long), zoneId (Long), status (String: PENDING), fertilizerType (String), startedAt (String).<br><br>**Scenario 2: Fertigation is stopped manually**<br>**Given** a POST request to `/api/v1/zones/{zoneId}/fertigation/stop` is received with a valid Bearer token<br>**When** the API validates an active fertigation session exists and dispatches the stop command<br>**Then** the API responds with 200 OK returning the session with attributes: fertigationId (Long), status (String: STOPPED), totalNutrientUsedLiters (Double).<br><br>**Scenario 3: Fertigation recipe is created**<br>**Given** a POST request to `/api/v1/zones/{zoneId}/fertigation/recipe` is received with attributes: fertilizerType (String), targetEc (Double), injectionRatio (Double), durationMinutes (Integer), recurrence (String, nullable)<br>**When** the API validates the target EC is within the safe range for the crop type<br>**Then** the API responds with 201 Created and returns the persisted recipe.<br><br>**Scenario 4: Fertigation start is rejected without active irrigation**<br>**Given** a POST request to `/api/v1/zones/{zoneId}/fertigation/start` is received for a zone with no active irrigation session<br>**When** the API validates the irrigation state<br>**Then** the API responds with 409 Conflict and returns an error payload indicating that active irrigation is required before fertigation.<br><br>**Scenario 5: Fertigation history is retrieved**<br>**Given** a GET request to `/api/v1/zones/{zoneId}/fertigation/history` is received with a valid Bearer token and optional query parameters: from (String), to (String), limit (Integer)<br>**When** the API retrieves past fertigation sessions<br>**Then** the API responds with 200 OK and returns a list where each item contains: fertigationId (Long), fertilizerType (String), status (String), startedAt (String), stoppedAt (String), nutrientUsedLiters (Double). | EP-002 |
| **EP-002-TS033** | Edge API Processes Fertigation Valve Commands | As a Cloud Developer, I want the Edge API to receive fertigation commands from the Cloud and execute them reliably so that nutrient injection is executed accurately on the physical hardware through the microcontroller. | **Scenario 1: Edge relays a fertigation open command to the device**<br>**Given** that the Cloud dispatches a fertigation start command for a specific device via REST<br>**When** the Edge receives and validates the instruction and confirms the irrigation valve for the zone is already open<br>**Then** the Edge relays the command to the ESP32 via MQTT, the device activates the fertigation electrovalve and dosing pump and starts the safety timer, and the Edge confirms execution back to the Cloud via REST.<br><br>**Scenario 2: Edge aborts fertigation when irrigation flow stops**<br>**Given** that a fertigation session is active and the irrigation valve closes or water flow is lost<br>**When** the Edge detects the loss of carrier water flow<br>**Then** the Edge immediately relays a fertigation close command to the ESP32 via MQTT and reports the interruption to the Cloud.<br><br>**Scenario 3: Edge handles fertigation command failure**<br>**Given** that the Edge attempts to relay a fertigation command to the device via MQTT but the device does not respond<br>**When** the relay fails after configured retry attempts<br>**Then** the Edge reports the failure status back to the Cloud with the error code. | EP-002 |
| **EP-002-TS034** | Embedded ESP32 Controls Fertigation Electrovalve and Dosing Pump | As an Edge Developer, I want the ESP32 firmware to receive fertigation open and close commands from the Edge and drive the fertigation electrovalve and dosing pump reliably so that fertilizer injection is activated and stopped correctly on the physical hardware. | **Scenario 1: Firmware executes a fertigation open command**<br>**Given** that the ESP32 receives a validated fertigation open command with an optional duration and injection ratio from the Edge via MQTT<br>**When** the firmware processes the command<br>**Then** the firmware activates the fertigation solenoid valve relay and the dosing pump relay, starts the internal hardware timer for the specified duration, and confirms the state change back to the Edge via MQTT.<br><br>**Scenario 2: Auto-close safety fallback when timer expires**<br>**Given** that the fertigation valve and dosing pump are active and communication with the Edge is interrupted<br>**When** the internal hardware timer reaches zero<br>**Then** the firmware closes the fertigation valve and stops the dosing pump automatically to prevent over-fertilization and salinity buildup, regardless of whether a stop command was received.<br><br>**Scenario 3: Firmware executes a fertigation close command**<br>**Given** that the ESP32 receives a validated fertigation close command from the Edge via MQTT while injection is active<br>**When** the firmware processes the command<br>**Then** the firmware deactivates the dosing pump and the fertigation solenoid valve relay, stops the hardware timer, and confirms the state change back to the Edge via MQTT. | EP-002 |
| **EP-003** | Perimeter Security and Early Warning | Protect farmers' investments from intruders and adverse environmental conditions through Cloud API, Edge API, Embedded, Web App, and Mobile App. | N/A (Es un Epic) | EP-003-US005, EP-003-US006, EP-003-US007, EP-003-TS011, EP-003-TS006, EP-003-TS010, EP-003-SS001, EP-003-SS002 |
| **EP-003-US005** | View Perimeter Security Events | As a farmer, I want to view the perimeter security events for my farm so that I can review a classified and chronological log of all intrusion attempts detected by the system. | **Scenario 1: Classified security events are displayed in chronological order**<br>**Given** that the farmer accesses the security module for a specific farm<br>**When** the system retrieves the recorded perimeter events<br>**Then** the system displays a chronological list showing the classification type (person, animal, wind), the confidence level, the severity, and the detection timestamp.<br><br>**Scenario 2: Events are filtered by classification type**<br>**Given** that the farmer applies a specific classification filter<br>**When** the system processes the filtered request<br>**Then** the system displays only the events whose classification matches the selected filter. | EP-003 |
| **EP-003-US006** | Receive Perimeter Intrusion Alert | As a farmer, I want to receive perimeter intrusion alerts so that I can respond immediately when a human presence is detected on my farm. | **Scenario 1: Critical alert is dispatched for human detection with high confidence**<br>**Given** that the system classifies a perimeter event as a human presence with high confidence<br>**When** the classification is confirmed<br>**Then** the system dispatches an immediate high-priority notification through the farmer's selected channels, including the event location, confidence level, and detection timestamp.<br><br>**Scenario 2: Low-priority events are logged silently**<br>**Given** that the system classifies an event as an animal or wind with low risk<br>**When** the evaluation occurs<br>**Then** the system logs the event in the security history without dispatching a critical alert, unless the farmer has explicitly configured alerts for all event types. | EP-003 |
| **EP-003-US007** | Configure Perimeter Security Settings | As a farmer, I want to configure the perimeter security settings for my farm so that I can adjust detection sensitivity and define active monitoring schedules according to my needs. | **Scenario 1: Detection sensitivity is adjusted**<br>**Given** that the farmer accesses the security settings for a specific farm<br>**When** the farmer modifies the motion sensitivity parameter and saves<br>**Then** the system applies the new sensitivity setting to the physical device and confirms the change was propagated.<br><br>**Scenario 2: Active monitoring schedule is defined**<br>**Given** that the farmer configures a specific monitoring time window with start and end times<br>**When** the farmer saves the settings<br>**Then** the system ensures that events outside the configured schedule are logged but do not trigger active notifications. | EP-003 |
| **EP-003-TS011** | Cloud RESTful API — Perimeter Security Endpoints | As a Frontend Developer, I want the Cloud API to implement RESTful endpoints for perimeter security so that the Web and Mobile applications can query and manage intrusion events. | **Scenario 1: Paginated security events are retrieved with filters**<br>**Given** a GET request to `/api/v1/farms/{farmId}/security/events` is received with a valid Bearer token and optional query parameters: from (String), to (String), severity (String), classification (String), limit (Integer)<br>**When** the API processes the authenticated request<br>**Then** the API responds with 200 OK returning a paginated JSON list of events with classifications, confidence scores, and timestamps.<br><br>**Scenario 2: Security event is acknowledged**<br>**Given** a PATCH request to `/api/v1/security/events/{eventId}` is received with attribute: acknowledged (Boolean: true)<br>**When** the API processes the acknowledgment<br>**Then** the API responds with 200 OK and returns the updated event with acknowledgedBy (Long) and acknowledgedAt (String).<br><br>**Scenario 3: Security event is ingested from the Edge**<br>**Given** a POST request to `/api/v1/security/events/ingest` is received with Edge device credentials and a security event payload<br>**When** the API validates and persists the event<br>**Then** the API responds with 201 Created and, if critical, triggers notification dispatch. | EP-003 |
| **EP-003-TS006** | Edge API Processes PIR Motion Classification Locally | As a Cloud Developer, I want the Edge API to run the motion classification algorithm locally so that latency is reduced, offline operation is guaranteed, and only critical detections are forwarded to the Cloud. | **Scenario 1: Edge classifies a perimeter event without cloud dependency**<br>**Given** that the Edge receives a motion trigger and raw sensor data from the embedded PIR device<br>**When** the Edge processes the event with the local classification model<br>**Then** the Edge produces a classification result (person, animal, wind) with a confidence score and stores the result locally.<br><br>**Scenario 2: Edge forwards critical human detection immediately**<br>**Given** that the classification result is Person with high confidence<br>**When** the Edge has active internet connectivity<br>**Then** the Edge dispatches the alert to the Cloud API immediately for notification routing.<br><br>**Scenario 3: Edge stores non-critical events for batch synchronization**<br>**Given** that the classification result is Animal or Wind<br>**When** the Edge processes the event<br>**Then** the Edge stores it locally and includes it in the next batch sync. | EP-003 |
| **EP-003-TS010** | Embedded ESP32 Processes PIR Motion Detection | As an Edge Developer, I want the ESP32 firmware to detect motion via the PIR sensor and capture the raw temporal data needed for classification so that the Edge can reliably classify the threat. | **Scenario 1: Firmware detects motion and captures classification data**<br>**Given** that the PIR sensor hardware detects a heat signature triggering the interrupt<br>**When** the firmware wakes from sleep in response<br>**Then** the firmware captures the signal intensity and temporal pattern readings over the sampling window, and transmits the raw data payload to the Edge for classification.<br><br>**Scenario 2: Firmware remains in low-power mode when no motion is detected**<br>**Given** that no motion is detected by the PIR sensor<br>**When** the device is in perimeter monitoring mode<br>**Then** the firmware maintains the ESP32 in deep sleep mode, waking only on PIR hardware interrupt, to minimize power consumption. | EP-003 |
| **EP-003-SS001** | Research Thermal PIR vs RGB Camera Accuracy and Energy Consumption | As a Device Maker, I want to research and compare the detection accuracy and energy consumption of a thermal PIR sensor versus an RGB camera module so that the optimal hardware selection can be made for the perimeter security MVP. | **Scenario 1: Field comparison tests are executed**<br>**Given** that technical data is required to define the security architecture<br>**When** the team runs comparative field tests classifying person, animal, and wind events under controlled conditions<br>**Then** a technical report is generated containing detection accuracy metrics and energy consumption measurements for both hardware options. | EP-003 |
| **EP-003-SS002** | Research ML Classification Model Size Executable on ESP32 | As a Device Maker, I want to research the maximum machine learning model size executable on the ESP32 hardware so that the AI architecture can be defined as on-device or Edge-based classification. | **Scenario 1: Model inference benchmarks are executed on target hardware**<br>**Given** that candidate classification models of different sizes are available for testing<br>**When** the team runs inference benchmarks on the ESP32 hardware<br>**Then** latency, RAM usage, flash memory consumption, and energy per inference metrics are documented in a technical report recommending on-device vs Edge classification. | EP-003 |
| **EP-004** | Mobile Field Operations and IoT Device Management | Enable farmers to operate, monitor, and control their IoT devices from mobile under real-world field conditions through Mobile App and Cloud API. | N/A (Es un Epic) | EP-004-US008, EP-004-US009, EP-004-US010, EP-004-US011, EP-004-US017, EP-004-US019, EP-004-TS023, EP-004-TS024, EP-004-TS025, EP-004-MS005 |
| **EP-004-US008** | Use Mobile App in Offline Mode | As a farmer, I want to use the mobile application in offline mode so that I can view the most recently synchronized sensor data even when I have no internet connection in the field. | **Scenario 1: Cached sensor data is displayed when offline**<br>**Given** that the mobile device has lost internet connection<br>**When** the farmer accesses the application<br>**Then** the application displays the most recent synchronized sensor data along with a timestamp indicating the last update and an offline status indicator.<br><br>**Scenario 2: Data is synchronized automatically when connection is restored**<br>**Given** that the application is operating in offline mode<br>**When** the internet connection is restored<br>**Then** the application synchronizes all pending data in the background and removes the offline status indicator. | EP-004 |
| **EP-004-US009** | Receive and Interact with Push Notifications Natively | As a farmer, I want to receive and interact with push notifications natively on my mobile device so that I can be alerted of critical conditions and take action directly from the notification without opening the application. | **Scenario 1: Actionable push notification is received**<br>**Given** that a critical system alert is generated by the Cloud notification service<br>**When** the notification reaches the mobile device via Firebase Cloud Messaging<br>**Then** the operating system displays a native notification containing the alert title, affected zone name, and quick-action buttons.<br><br>**Scenario 2: Push notification permission is requested on first login**<br>**Given** that the farmer logs into the application for the first time on a new device<br>**When** the application initializes<br>**Then** the application explains the benefit of receiving push notifications and requests permission from the operating system. | EP-004 |
| **EP-004-US010** | Open Irrigation Valve from Push Alert | As a farmer, I want to activate the irrigation valve directly from a push notification alert so that I can respond to water stress immediately without navigating through the application. | **Scenario 1: Valve is activated from notification quick action**<br>**Given** that the farmer receives a water stress notification for a specific zone<br>**When** the farmer triggers the valve activation quick action from the notification<br>**Then** the application dispatches the valve open command to the Cloud API, confirms execution, and updates the local state.<br><br>**Scenario 2: Valve activation is queued when device is offline**<br>**Given** that the farmer triggers valve activation from a notification while the device is offline<br>**When** the application cannot reach the Cloud API<br>**Then** the application stores the command in the offline queue and dispatches it when connectivity is restored within the safety timeframe. | EP-004 |
| **EP-004-US011** | View Live Valve and Irrigation Status from Mobile | As a farmer, I want to view the live valve status and current irrigation state from the mobile application so that I can know the operational state of my field at any moment. | **Scenario 1: Real-time valve status is displayed for a zone**<br>**Given** that the farmer accesses the irrigation control section for a specific zone<br>**When** the application retrieves the current valve and session status from the Cloud API<br>**Then** the application displays the valve state, elapsed irrigation time, and estimated water consumption.<br><br>**Scenario 2: Status is refreshed by polling during active irrigation**<br>**Given** that an irrigation session is active for a zone<br>**When** the application periodically polls the irrigation status endpoint<br>**Then** the application refreshes elapsed time and water consumption without requiring manual user interaction. | EP-004 |
| **EP-004-US017** | Register and Activate IoT Device | As a farmer, I want to register and activate a new IoT device so that I can link physical sensors to my digital account and begin receiving telemetry data. | **Scenario 1: Device is registered by entering the serial number**<br>**Given** that the farmer initiates the device setup wizard and enters the device serial number and type<br>**When** the system verifies the serial number and creates the device record<br>**Then** the system registers the device in the farmer's account and confirms that it is pending activation.<br><br>**Scenario 2: Device is activated via QR code scanning**<br>**Given** that the farmer scans the device QR code containing the factory certificate<br>**When** the system validates the certificate integrity<br>**Then** the system provisions unique operational credentials, links the device to the account, and confirms the first telemetry reception.<br><br>**Scenario 3: Duplicate serial number is rejected**<br>**Given** that the farmer enters a serial number already registered in the system<br>**When** the system validates the serial number<br>**Then** the system rejects the registration and indicates that the serial number is already registered. | EP-004 |
| **EP-004-US019** | View Device Connectivity Status | As a farmer, I want to view the connectivity status of all my IoT devices so that I can proactively identify sensors that are offline and take corrective action. | **Scenario 1: Device fleet status is displayed with connectivity indicators**<br>**Given** that the farmer accesses the device management list<br>**When** the system retrieves the fleet status from the Cloud API<br>**Then** the system displays each device with its serial number, type, connectivity status, battery level, and the timestamp of the last known reading.<br><br>**Scenario 2: Detailed device connectivity information is retrieved**<br>**Given** that the farmer requests detailed information for a specific device<br>**When** the system retrieves the connection details<br>**Then** the system displays the signal strength, firmware version, uptime, and historical connection events for that device. | EP-004 |
| **EP-004-TS023** | Configure Firebase Cloud Messaging for Push Notifications | As a Mobile Developer, I want to integrate Firebase Cloud Messaging in the Android and iOS applications so that farmers receive reliable push notifications even when the application is closed or in the background. | **Scenario 1: FCM device token is registered on application startup**<br>**Given** that the farmer logs into the application for the first time on a device<br>**When** the application initializes<br>**Then** the application requests notification permission, obtains the FCM registration token, and sends it to the Cloud API to associate it with the user profile.<br><br>**Scenario 2: Push notification is received when app is in background**<br>**Given** that the application is in the background or closed<br>**When** FCM delivers a notification to the device<br>**Then** the OS displays the notification natively with the title, body, and quick actions from the payload.<br><br>**Scenario 3: FCM token refresh is handled automatically**<br>**Given** that the FCM registration token is refreshed by the Firebase platform<br>**When** the new token is generated<br>**Then** the application automatically sends the new token to the Cloud API without user action. | EP-004 |
| **EP-004-TS024** | Implement Deep Linking from Notification to Target Screen | As a Mobile Developer, I want to implement deep linking from push notifications to the specific target screen so that the farmer can navigate directly to the affected zone with a single tap. | **Scenario 1: Tapping notification opens the correct zone control screen**<br>**Given** that the farmer receives a water stress notification for a specific zone<br>**When** the farmer taps the notification<br>**Then** the application opens and navigates directly to that zone's control screen with soil status and action buttons visible.<br><br>**Scenario 2: Quick action from locked screen executes with biometric confirmation**<br>**Given** that the device is locked and a critical irrigation alert arrives<br>**When** the farmer selects the quick action on the lock screen<br>**Then** the application requests biometric authentication and, upon confirmation, executes the command. | EP-004 |
| **EP-004-TS025** | Implement Offline Command Queue in Mobile App | As a Mobile Developer, I want the mobile application to manage an offline command queue so that irrigation commands requested while offline are automatically dispatched when connectivity is restored. | **Scenario 1: Irrigation command is queued while device is offline**<br>**Given** that the mobile device has no internet connectivity<br>**When** the farmer triggers an irrigation command<br>**Then** the application stores the command locally with the action, target zone, timestamp, and expiration time.<br><br>**Scenario 2: Queued commands are dispatched upon reconnection**<br>**Given** that the device regains internet connectivity<br>**When** the application detects network restoration<br>**Then** the application processes the offline queue in FIFO order, dispatching each command to the Cloud API.<br><br>**Scenario 3: Expired offline commands are discarded**<br>**Given** that a queued command exceeds its safety expiration timeframe<br>**When** the application processes the offline queue<br>**Then** the application discards the expired command and notifies the farmer. | EP-004 |
| **EP-004-MS005** | Implement Zero-Touch Device Provisioning | As a Cloud Developer, I want the Cloud API to support zero-touch device provisioning so that IoT hardware activation via QR code scanning is secure and automated without manual configuration. | **Scenario 1: Device is securely provisioned via QR code scan**<br>**Given** that a new physical device needs activation and the farmer scans the QR code<br>**When** the Cloud API receives the factory certificate and device identifier<br>**Then** the API validates the certificate integrity, provisions unique operational credentials, registers the device in the active fleet, and returns a confirmation.<br><br>**Scenario 2: Unauthenticated device provisioning attempt is rejected**<br>**Given** that a device without valid factory credentials attempts to provision<br>**When** the API validates the certificate<br>**Then** the API responds with 401 Unauthorized and logs the attempt as a security event. | EP-004 |
| **EP-005** | IoT and Edge Infrastructure | Ensure the technical reliability of devices, connectivity, and data flow in the field through Edge API, Embedded, and Cloud API. | N/A (Es un Epic) | EP-005-TS004, EP-005-TS007, EP-005-TS008, EP-005-TS009, EP-005-TS010, EP-005-TS011, EP-005-TS013, EP-005-MS001, EP-005-MS006 |
| **EP-005-TS004** | Edge API Receives and Validates Sensor Telemetry | As a Cloud Developer, I want the Edge API to receive sensor telemetry from IoT devices and store it locally so that data persistence is guaranteed even during intermittent internet connectivity. | **Scenario 1: Edge receives and stores periodic telemetry from IoT devices**<br>**Given** that the IoT device transmits sensor readings periodically to the Edge via MQTT<br>**When** the Edge receives and processes the telemetry payload<br>**Then** the Edge validates that each reading falls within physically possible ranges, stores the data locally, and confirms receipt to the device via MQTT.<br><br>**Scenario 2: Edge rejects invalid sensor readings**<br>**Given** that the IoT device transmits a sensor reading outside the physically possible range via MQTT<br>**When** the Edge validates the payload<br>**Then** the Edge rejects the invalid reading, logs the anomaly locally, and marks the sensor health as degraded. | EP-005 |
| **EP-005-TS007** | Edge Database Manages Local Storage Under Connectivity Constraints | As a Cloud Developer, I want the Edge database to manage local storage with a defined retention policy so that memory overflow is avoided during extended offline periods. | **Scenario 1: Retention policy is applied when storage limit is reached**<br>**Given** that the Edge has been offline for an extended period and storage exceeds the configured threshold<br>**When** the system evaluates the local retention policy<br>**Then** the Edge retains critical events and alerts, discards old routine telemetry beyond the retention period, and logs the purge action locally. | EP-005 |
| **EP-005-TS008** | Edge-to-Cloud Synchronization Protocol | As a Cloud Developer, I want the Edge to implement a synchronization protocol with delivery guarantee so that no telemetry data is lost during intermittent network connectivity. | **Scenario 1: Edge synchronizes pending data in batches upon reconnection**<br>**Given** that the Edge has locally stored telemetry data pending sync<br>**When** internet connectivity is restored<br>**Then** the Edge dispatches pending data in batches to the Cloud, waits for batch-level acknowledgment, and clears each batch from the local buffer only upon receiving confirmation.<br><br>**Scenario 2: Edge handles partial batch synchronization failure**<br>**Given** that a transmitted batch is only partially acknowledged by the Cloud<br>**When** the Edge receives the partial success response<br>**Then** the Edge marks acknowledged records as synced and retains failed records for retry in the next cycle. | EP-005 |
| **EP-005-TS009** | Edge API Authentication for IoT Devices (mTLS) | As a Cloud Developer, I want the Edge API to implement mutual TLS authentication between IoT devices and the Edge so that only authorized and provisioned devices can communicate. | **Scenario 1: Device authenticates successfully with valid client certificate**<br>**Given** that a provisioned IoT device with an embedded client certificate attempts to connect to the Edge<br>**When** the Edge validates the certificate against the authorized device registry<br>**Then** the Edge establishes a secure mutual TLS channel and accepts telemetry data.<br><br>**Scenario 2: Edge rejects unauthorized or revoked device**<br>**Given** that an unprovisioned device or a device with a revoked certificate attempts to connect<br>**When** the Edge validates the certificate<br>**Then** the Edge rejects the connection, logs the attempt as a security event, and queues the event for Cloud sync. | EP-005 |
| **EP-005-TS010** | Local Rule Engine on Edge | As a Cloud Developer, I want the Edge to evaluate threshold rules locally so that critical actions are guaranteed even when the Edge has no cloud connectivity. | **Scenario 1: Local rule triggers valve closure on threshold breach while offline**<br>**Given** that the Edge operates without a cloud connection<br>**When** a new sensor reading violates a preconfigured automatic action threshold<br>**Then** the local rule engine triggers the corresponding physical command immediately and queues the event for later cloud synchronization.<br><br>**Scenario 2: Edge synchronizes updated rule configurations from Cloud**<br>**Given** that the Edge reconnects to the Cloud after an offline period<br>**When** the Edge fetches updated rule configurations from the Cloud API<br>**Then** the Edge updates its local rule engine with the latest thresholds and policies. | EP-005 |
| **EP-005-TS011** | Edge Heartbeat and Health Reporting | As a Cloud Developer, I want the Edge to transmit periodic heartbeats so that the Cloud monitoring service can detect offline nodes automatically and trigger support alerts. | **Scenario 1: Edge transmits heartbeat within the expected interval**<br>**Given** that the Edge sends a heartbeat to the Cloud API within the configured interval<br>**When** the Cloud monitoring service receives the heartbeat<br>**Then** the Cloud updates the Edge node status to ONLINE and records the reception timestamp.<br><br>**Scenario 2: Cloud triggers alert when heartbeat is missed**<br>**Given** that an Edge node fails to transmit a heartbeat within the expected timeframe<br>**When** the Cloud monitoring service detects the absence beyond the grace period<br>**Then** the Cloud marks the node as OFFLINE and dispatches a technical support alert. | EP-005 |
| **EP-005-TS013** | IoT Gateway Auto-Reconnection Protocol | As a Cloud Developer, I want the Edge to implement an automatic reconnection protocol with a command queue so that solenoid valve commands are guaranteed to execute even with network intermittency. | **Scenario 1: Edge recovers automatically after network failure**<br>**Given** that the Edge gateway loses network connectivity<br>**When** the physical link is restored<br>**Then** the Edge executes an exponential backoff routine to reconnect, synchronizes any pending commands from the queue, and executes critical actions without saturating the server.<br><br>**Scenario 2: Edge replays missed commands in correct order**<br>**Given** that commands were queued during the offline period<br>**When** reconnection is established<br>**Then** the Edge replays queued commands in FIFO order, respecting original timestamps and expiration policies. | EP-005 |
| **EP-005-MS001** | Implement Asynchronous Event Processing Queue for IoT Telemetry | As a Cloud Developer, I want the Cloud API to implement an asynchronous message queue for incoming telemetry so that ingestion is decoupled from rule processing and data loss is avoided during high-volume bursts. | **Scenario 1: Telemetry burst is processed without data loss**<br>**Given** that multiple Edge devices transmit telemetry simultaneously during a high-volume period<br>**When** the messages reach the Cloud ingestion endpoint<br>**Then** the system acknowledges persistence immediately, queues messages in order, and processes them asynchronously through the rule engine.<br><br>**Scenario 2: Failed telemetry message is routed to dead letter queue**<br>**Given** that a telemetry message fails processing after all retry attempts<br>**When** the maximum retry count is exhausted<br>**Then** the system moves the message to a dead letter queue and logs the failure for review. | EP-005 |
| **EP-005-MS006** | Implement Over-The-Air Firmware Update (OTA) | As a Cloud Developer, I want the Cloud API to support Over-The-Air firmware updates for IoT devices so that software improvements and security patches can be distributed remotely without field visits. | **Scenario 1: Firmware binary is uploaded and validated for distribution**<br>**Given** a POST request to `/api/v1/admin/firmware` is received with a valid admin Bearer token, the firmware binary, and metadata: version (String), deviceModel (String), changelog (String)<br>**When** the API validates the binary checksum and version compatibility<br>**Then** the API responds with 201 Created and makes the firmware available for staged rollout.<br><br>**Scenario 2: IoT device downloads and applies the firmware update**<br>**Given** that a new validated firmware version is available for the device model<br>**When** the API dispatches an update notification to the target device<br>**Then** the device downloads the binary, verifies its checksum, applies the update, and confirms the new version to the Cloud.<br><br>**Scenario 3: Firmware update fails and device rolls back**<br>**Given** that a firmware update fails due to checksum mismatch or flash error<br>**When** the device detects the failure<br>**Then** the device automatically rolls back to the previous firmware version and reports the failure to the Cloud. | EP-005 |
| **EP-006** | Subscriptions, Billing and Revenue Operations | Manage service monetization, the subscription lifecycle, and automatic billing through Cloud API and Web App. | N/A (Es un Epic) | EP-006-US014, EP-006-US015, EP-006-US016, EP-006-TS017 |
| **EP-006-US014** | View and Manage Subscription Plan | As a farmer, I want to view and manage my subscription plan so that I can evaluate my current tier, included features, and available upgrade options. | **Scenario 1: Current subscription details are displayed**<br>**Given** that the farmer accesses the account settings<br>**When** the subscription information is loaded<br>**Then** the system displays the active plan name, price, covered hectares and devices, next billing date, and included features.<br><br>**Scenario 2: Plan is upgraded from Basic to Pro**<br>**Given** that the farmer selects a higher-tier plan and confirms the change<br>**When** the system processes the payment for the new plan<br>**Then** the system activates the new features immediately and confirms the prorated billing adjustment. | EP-006 |
| **EP-006-US015** | Manage Billing and Payment Methods | As a farmer, I want to manage my billing information and payment methods so that I can access my payment history and update payment details as needed. | **Scenario 1: Payment history is displayed chronologically**<br>**Given** that the farmer accesses the billing section<br>**When** the billing history is retrieved<br>**Then** the system presents a chronological list of payments with amounts, dates, status, and the option to download PDF invoices.<br><br>**Scenario 2: System handles a failed automatic payment**<br>**Given** that an automatic payment fails due to insufficient funds or expired method<br>**When** the system detects the failure<br>**Then** the system notifies the farmer, prompts for a payment method update, and maintains service in a temporary grace period. | EP-006 |
| **EP-006-US016** | Cancel Subscription and Request Hardware Return | As a farmer, I want to cancel my subscription and request the return of IoT hardware so that the offboarding process is handled securely and transparently. | **Scenario 1: Subscription cancellation is initiated**<br>**Given** that the farmer selects to cancel the active subscription and confirms<br>**When** the system processes the cancellation<br>**Then** the system sets the deactivation date to the end of the billing cycle and optionally records the exit reason.<br><br>**Scenario 2: Offboarding email is dispatched after cancellation**<br>**Given** that the cancellation has been processed successfully<br>**When** the system updates the subscription status<br>**Then** the system dispatches an automated email containing account closure steps, data retention policy, and hardware return instructions. | EP-006 |
| **EP-006-TS017** | Cloud RESTful API — Subscriptions and Billing Endpoints | As a Frontend Developer, I want the Cloud API to implement subscription and billing endpoints integrated with an external payment provider so that the Web Application can manage plans, payments, and billing history. | **Scenario 1: Current subscription details are retrieved**<br>**Given** a GET request to `/api/v1/subscriptions/current` is received with a valid Bearer token<br>**When** the API retrieves the user's active subscription<br>**Then** the API responds with 200 OK returning: subscriptionId (Long), planName (String), status (String), maxFarms (Integer), maxDevices (Integer), features (List), startDate (String), endDate (String), nextBillingDate (String).<br><br>**Scenario 2: Payment provider webhook confirms successful payment**<br>**Given** a POST request to `/api/v1/webhooks/payments` is received from the external payment provider with a successful payment event<br>**When** the API validates the webhook cryptographic signature<br>**Then** the API updates the subscription status to ACTIVE, extends the billing date, and logs the transaction.<br><br>**Scenario 3: Payment provider webhook notifies a failed payment**<br>**Given** a POST request to `/api/v1/webhooks/payments` is received with a payment failure event<br>**When** the API processes the webhook<br>**Then** the API activates the grace period flag, notifies the customer, and schedules automatic payment retries.<br><br>**Scenario 4: Plan is selected or upgraded**<br>**Given** a POST request to `/api/v1/subscriptions` is received with attributes: planName (String), paymentMethodId (String), billingCycle (String)<br>**When** the API processes the payment through the external provider<br>**Then** the API responds with 201 Created returning the new subscription with prorated billing adjustment.<br><br>**Scenario 5: Billing history is retrieved**<br>**Given** a GET request to `/api/v1/billing/history` is received with a valid Bearer token and optional query parameters: from (String), to (String), limit (Integer)<br>**When** the API retrieves billing records<br>**Then** the API responds with 200 OK returning a list where each item contains: invoiceId (Long), planName (String), amount (Double), currency (String), status (String), billingDate (String), invoiceUrl (String).<br><br>**Scenario 6: Available plans are retrieved**<br>**Given** a GET request to `/api/v1/subscriptions/plans` is received<br>**When** the API retrieves the plan catalog<br>**Then** the API responds with 200 OK returning a list with planName (String), price (Double), currency (String), billingCycles (List), features (List). | EP-006 |
| **EP-007** | Software Quality and User Experience | Verify the robustness, security, accessibility, and performance of the entire technology platform across all layers. | N/A (Es un Epic) | EP-007-TS021, EP-007-TS022, EP-007-TS027, EP-007-TS028, EP-007-MS002, EP-007-MS003, EP-007-MS004, EP-007-MS014 |
| **EP-007-TS021** | Implement Lazy Loading and Code Splitting for Dashboard Modules | As a Frontend Developer, I want the Web Application to implement lazy loading and code splitting for dashboard modules so that the initial page load time is minimized. | **Scenario 1: Only the active module is loaded on initial render**<br>**Given** that the farmer accesses the dashboard<br>**When** the Web Application initializes<br>**Then** the application loads only the module for the active view and defers loading of other modules until navigation occurs.<br><br>**Scenario 2: Multi-parcel view renders efficiently with virtualization**<br>**Given** that an agronomist with many client parcels accesses the dashboard<br>**When** the multi-parcel view loads<br>**Then** the application renders only the parcels currently visible in the viewport using virtualization. | EP-007 |
| **EP-007-TS022** | Implement Global State Management for the Dashboard | As a Frontend Developer, I want the Web Application to implement a global state manager so that sensor data, alerts, and configuration are synchronized across components without redundant API calls. | **Scenario 1: State is synchronized across SPA components**<br>**Given** that a telemetry update is received via REST polling<br>**When** the global state store is updated with the new data<br>**Then** all active UI components depending on that data slice re-render automatically without additional HTTP requests.<br><br>**Scenario 2: State cache is invalidated after a user configuration change**<br>**Given** that the farmer modifies a configuration setting<br>**When** the API confirms the successful update<br>**Then** the global state manager invalidates the affected data slices and triggers a single data refresh. | EP-007 |
| **EP-007-TS027** | Internationalization Support (i18n) | As a Frontend Developer, I want the Landing Page and Web Application to support internationalization so that the platform can be used comfortably in the user's preferred language without barriers. | **Scenario 1: User switches language preference**<br>**Given** that the platform supports multiple languages with English as the default<br>**When** the user updates the language preference<br>**Then** the Cloud API saves the preference and the entire application content is displayed in the selected language.<br><br>**Scenario 2: Dates, numbers, and amounts are formatted according to locale**<br>**Given** that a specific language and region are configured for the user<br>**When** the system displays dates, numeric values, or monetary amounts<br>**Then** the format corresponds to the conventions of the selected region. | EP-007 |
| **EP-007-TS028** | Accessibility Compliance (a11y) | As a Frontend Developer, I want the Web Application and Landing Page to comply with accessibility standards so that users with disabilities can access all platform features without barriers. | **Scenario 1: Application is compatible with screen readers**<br>**Given** that a user accesses the platform using a screen reader<br>**When** the user navigates through the application<br>**Then** all interactive elements are identifiable and operable via ARIA labels and text descriptions conveying their purpose and state.<br><br>**Scenario 2: Critical information is not conveyed by color alone**<br>**Given** that the system communicates critical states or alerts<br>**When** the information is rendered<br>**Then** it is conveyed through multiple indicators (text labels, iconography, patterns) ensuring users who cannot perceive color can still understand the criticality. | EP-007 |
| **EP-007-MS002** | Implement Auto-Scaling for Backend Services | As a Cloud Developer, I want the cloud infrastructure to configure horizontal auto-scaling for backend services so that performance is maintained during peak agricultural monitoring loads without unnecessary costs during low-demand periods. | **Scenario 1: Backend scales out when CPU exceeds threshold**<br>**Given** that the CPU usage exceeds 70% for a sustained period<br>**When** the auto-scaling controller evaluates the metrics<br>**Then** the system automatically provisions new service instances and redistributes incoming traffic.<br><br>**Scenario 2: Backend scales in during low-traffic periods**<br>**Given** that the CPU usage drops below 30% for a sustained period<br>**When** the auto-scaling controller runs the evaluation<br>**Then** the system terminates excess instances gracefully, allowing active connections to drain before shutdown. | EP-007 |
| **EP-007-MS003** | Implement Data Retention and Archiving Policy | As a Cloud Developer, I want the Cloud API to implement a historical data retention and archiving policy so that storage costs are balanced with data availability for long-term agronomic analysis. | **Scenario 1: Old telemetry data is archived to low-cost storage**<br>**Given** that telemetry readings exceed the active storage timeframe of 12 months<br>**When** the scheduled archiving process executes<br>**Then** the system moves the records to low-cost archival storage while maintaining searchable indexes.<br><br>**Scenario 2: Data is permanently purged after absolute retention limit**<br>**Given** that archived data exceeds the absolute retention limit of 5 years<br>**When** the purge policy evaluates the records<br>**Then** the system permanently deletes the data and logs the deletion event in the audit trail. | EP-007 |
| **EP-007-MS004** | Implement Automated Backup and Recovery | As a Cloud Developer, I want the cloud infrastructure to configure automatic backups of the main database so that data can be recovered after a catastrophic failure with minimal data loss. | **Scenario 1: Daily backup completes and is verified successfully**<br>**Given** that the backup process runs on the configured daily schedule<br>**When** the backup job completes<br>**Then** the system verifies backup integrity via checksum, stores the backup in a geographically separate secure region, and logs the completion event.<br><br>**Scenario 2: Recovery from backup is validated periodically**<br>**Given** that a periodic recovery test is executed in an isolated test environment<br>**When** the backup is restored<br>**Then** the system verifies that data is intact, constraints are preserved, and the documented Recovery Time Objective is met. | EP-007 |
| **EP-007-MS014** | Setup CI/CD Pipeline for Modular Backend | As a Cloud Developer, I want the repository to configure a CI/CD pipeline so that compilation, automated testing, and deployment of backend modules are executed safely and consistently on every code change. | **Scenario 1: Automated build and test suite runs on code integration**<br>**Given** that a code modification is integrated into the main branch via a pull request merge<br>**When** the CI/CD pipeline is triggered<br>**Then** the system executes compilation of all modules, runs the full unit test suite, and executes integration tests automatically.<br><br>**Scenario 2: Deployment is blocked when the test suite fails**<br>**Given** that the automated test suite returns one or more test failures<br>**When** the deployment step is evaluated by the pipeline<br>**Then** the pipeline halts execution, prevents deployment to any environment, and notifies the development team with failure details. | EP-007 |
| **EP-008** | Notifications and Multi-Channel Communication | Deliver timely, reliable, and personalized alerts across Push, WhatsApp, email, and SMS through Cloud API, Web App, and Mobile App. | N/A (Es un Epic) | EP-008-US020, EP-008-US021, EP-008-TS018 |
| **EP-008-US020** | Manage Advanced Notification Preferences | As a farmer, I want to manage advanced notification preferences so that I can route different types of alerts only to the communication channels I choose and avoid notification fatigue. | **Scenario 1: Notification channels are configured by alert type**<br>**Given** that the farmer accesses the notification settings<br>**When** the farmer enables or disables specific channels for each alert category<br>**Then** the system saves the preferences and routes future alerts strictly through the selected channels.<br><br>**Scenario 2: Daily summary digest is dispatched at the scheduled time**<br>**Given** that the farmer has opted in for a daily summary digest<br>**When** the scheduled system time for the digest arrives<br>**Then** the system dispatches a consolidated message containing the day's soil conditions, irrigation activities, and security events. | EP-008 |
| **EP-008-US021** | Receive WhatsApp Alert with Direct Action Link | As a farmer, I want to receive WhatsApp alerts with direct action links so that I can address critical field conditions immediately from my messaging application without opening the AgroSafe app. | **Scenario 1: WhatsApp alert is dispatched with deep link and plain-language summary**<br>**Given** that a critical water stress or security alert is generated<br>**When** the notification dispatch service processes the alert for WhatsApp delivery<br>**Then** the system sends a WhatsApp message containing a plain-language summary of the alert, the affected zone or device, and a secure deep link to the relevant screen in the mobile app.<br><br>**Scenario 2: Alert is escalated to SMS when WhatsApp delivery fails**<br>**Given** that a WhatsApp message is not confirmed as delivered within the configured timeout<br>**When** the dispatch service detects the delivery failure<br>**Then** the system automatically escalates by sending a backup SMS with the same critical information. | EP-008 |
| **EP-008-TS018** | Cloud RESTful API — Multi-Channel Notification Dispatch Service | As a Frontend Developer, I want the Cloud API to implement a multi-channel notification dispatch service so that internal modules can queue alerts for delivery through Push, WhatsApp, email, and SMS without each module implementing its own dispatch logic. | **Scenario 1: Notification is queued and dispatched to the preferred channel**<br>**Given** a POST request to `/api/v1/notifications/dispatch` is received from an internal module with attributes: userId (Long), title (String), message (String), category (String), priority (String), channels (List), actionUrl (String, nullable)<br>**When** the dispatch service processes the queue<br>**Then** the system identifies the user's preferred channel, executes the API call to the external provider, and marks the notification as sent.<br><br>**Scenario 2: Notification dispatch fails and is requeued with backoff**<br>**Given** that the external provider returns an error during dispatch<br>**When** the dispatch service processes the failure<br>**Then** the system logs the failure and requeues the notification with exponential backoff before the next retry.<br><br>**Scenario 3: User's notifications are retrieved with pagination**<br>**Given** a GET request to `/api/v1/notifications` is received with a valid Bearer token and optional query parameters: read (Boolean), category (String), limit (Integer), before (String)<br>**When** the API retrieves notifications<br>**Then** the API responds with 200 OK returning a list where each item contains: notificationId (Long), title (String), message (String), category (String), priority (String), read (Boolean), createdAt (String), actionUrl (String).<br><br>**Scenario 4: Notification is marked as read**<br>**Given** a PATCH request to `/api/v1/notifications/{notificationId}` is received with attribute: read (Boolean: true)<br>**When** the API updates the read status<br>**Then** the API responds with 200 OK and returns: notificationId (Long), read (Boolean), readAt (String). | EP-008 |
| **EP-009** | Agronomist Consulting and Multi-Client Management | Empower agronomists to remotely supervise multiple client parcels, standardize recommendations, and deliver data-driven consulting through Cloud API and Web App. | N/A (Es un Epic) | EP-009-US001, EP-009-US002, EP-009-US003, EP-009-US004, EP-009-US005, EP-009-TS016, EP-009-MS012, EP-009-US006, EP-009-US007 |
| **EP-009-US001** | View Multi-Parcel Dashboard | As an agronomist, I want to view a consolidated multi-parcel dashboard so that I can monitor all my clients' parcels from a single view and quickly identify which ones require my immediate attention. | **Scenario 1: Overview of all client parcels is displayed with status indicators**<br>**Given** that the agronomist has multiple active farmer clients with linked farms<br>**When** the agronomist accesses the main dashboard<br>**Then** the system displays a consolidated view with status indicators for every associated parcel, highlighting those with active alerts or critical sensor readings.<br><br>**Scenario 2: Quick parcel details are accessed without full navigation**<br>**Given** that the agronomist is viewing the parcel summary list<br>**When** quick information about a specific parcel is requested<br>**Then** the system presents key metrics and active alert statuses without requiring navigation to the full details view. | EP-009 |
| **EP-009-US002** | Adjust Client Irrigation Thresholds Remotely | As an agronomist, I want to adjust my clients' irrigation thresholds remotely so that I can update crop-specific recommendations without having to make a physical visit to the field. | **Scenario 1: Thresholds are updated for a single client parcel**<br>**Given** that the agronomist manages a specific client parcel's irrigation settings<br>**When** the agronomist modifies the threshold values and confirms<br>**Then** the system applies the new values, notifies the associated farmer, and logs the change in the audit trail.<br><br>**Scenario 2: Thresholds are bulk-updated for multiple similar parcels**<br>**Given** that the agronomist selects multiple parcels with the same crop type<br>**When** the agronomist applies a threshold change globally<br>**Then** the system processes the update for each parcel simultaneously, reports successes and failures, and notifies each affected farmer. | EP-009 |
| **EP-009-US003** | Generate Technical Report for Client | As an agronomist, I want to generate a technical report for a client so that I can share monthly soil health summaries, historical trends, and agronomic recommendations in a professional document format. | **Scenario 1: Monthly soil health report is generated**<br>**Given** that the agronomist requests a report for a specific client farm, selecting report type and time period<br>**When** the system compiles sensor data, historical trends, applied recommendations, and alert history<br>**Then** the system generates the report as an exportable document and makes it available for download.<br><br>**Scenario 2: Report is shared directly with the client**<br>**Given** that a technical report has been generated successfully<br>**When** the agronomist triggers the share action<br>**Then** the system dispatches a message to the client containing a summary and a secure link to view or download the full document. | EP-009 |
| **EP-009-US004** | Monitor Remote Parcels Between Visits | As an agronomist, I want to monitor my clients' remote parcels between scheduled visits so that I can dispatch proactive recommendations when critical environmental changes are detected by the sensors. | **Scenario 1: Critical alert is received for a client's parcel**<br>**Given** that a sensor on a client's parcel detects a telemetry value exceeding the critical threshold<br>**When** the system evaluates the rules and determines attention is required<br>**Then** the system dispatches an alert to the assigned agronomist detailing the out-of-range parameter, current value, historical context, and parcel identification.<br><br>**Scenario 2: Recommendation is dispatched from the alert context**<br>**Given** that the agronomist evaluates an incoming critical alert for a client<br>**When** the agronomist formulates a professional recommendation based on sensor data<br>**Then** the system routes the recommendation directly to the farmer accompanied by the supporting sensor data. | EP-009 |
| **EP-009-US005** | Configure Crop-Specific Thresholds as Agronomist Templates | As an agronomist, I want to configure and save crop-specific threshold templates so that I can quickly apply standardized agronomic settings to new client parcels without reconfiguring parameters manually each time. | **Scenario 1: A custom threshold template is created**<br>**Given** that the agronomist accesses the template management module<br>**When** the agronomist defines moisture, temperature, pH, and EC thresholds for a specific crop type and saves as a named template<br>**Then** the system persists the template and associates it with the agronomist's account for future use.<br><br>**Scenario 2: A saved template is applied to a client parcel**<br>**Given** that the agronomist has at least one saved threshold template<br>**When** the agronomist selects a template and applies it to a specific client parcel<br>**Then** the system overwrites the current thresholds with the template values, notifies the farmer, and records the action in the audit trail. | EP-009 |
| **EP-009-US006** | Link Farmer Client to Agronomist | As an agronomist, I want to link a farmer as a client to my account so that I can access their farm data and manage their agronomic configuration. | **Scenario 1: Farmer is linked as a client via email invitation**<br>**Given** that the agronomist sends a client linking request with the farmer's email and relationship type<br>**When** the system validates the farmer account exists and no active relationship exists<br>**Then** the system creates a client relationship with PENDING status and sends an invitation to the farmer.<br><br>**Scenario 2: Client list is displayed with filtering options**<br>**Given** that the agronomist has linked farmer clients<br>**When** the agronomist accesses the client list with optional status and search filters<br>**Then** the system displays the list with each client's name, email, relationship type, status, farm count, and last activity timestamp.<br><br>**Scenario 3: Already linked farmer is rejected**<br>**Given** that the agronomist attempts to link a farmer who already has an active client relationship<br>**When** the system validates the relationship status<br>**Then** the system prevents duplication and returns a conflict error. | EP-009 |
| **EP-009-US007** | View and Manage Client Recommendations | As an agronomist, I want to view and manage the recommendations I have created for a client so that I can track which advice has been acknowledged and follow up on pending recommendations. | **Scenario 1: Recommendations are retrieved for a client**<br>**Given** that the agronomist accesses the recommendations for a specific client with optional status, category, and priority filters<br>**When** the system retrieves the recommendations<br>**Then** the system displays the list with title, description, category, priority, status, associated farm or zone, and creation and acknowledgment timestamps.<br><br>**Scenario 2: A new recommendation is created for a client**<br>**Given** that the agronomist creates a recommendation with title, description, category, priority, and optional farm/zone association<br>**When** the system validates and persists the recommendation<br>**Then** the system creates the recommendation with status OPEN and notifies the farmer through the preferred channel. | EP-009 |
| **EP-009-TS016** | Cloud RESTful API — Agronomic Diagnosis Engine | As a Frontend Developer, I want the Cloud API to implement an agronomic diagnosis endpoint that combines soil telemetry with external weather context so that the Web Application can display automatic recommendations and computed water stress indices. | **Scenario 1: Diagnosis is generated on demand for a zone**<br>**Given** a POST request to `/api/v1/diagnosis` is received with a valid Bearer token and attribute: zoneId (Long)<br>**When** the API combines soil readings with meteorological data from the external weather service<br>**Then** the API responds with 200 OK returning: diagnosisId (Long), zoneId (Long), waterStressIndex (Double), soilHealthScore (Integer, 0-100), recommendations (List), weatherContextAvailable (Boolean), generatedAt (String).<br><br>**Scenario 2: Diagnosis is automatically triggered when threshold is breached**<br>**Given** that a telemetry reading for a zone exceeds a critical threshold<br>**When** the rules engine evaluates the condition<br>**Then** the system generates a diagnosis automatically and queues a notification for the assigned agronomist.<br><br>**Scenario 3: Soil trend analysis is retrieved for a zone**<br>**Given** a GET request to `/api/v1/zones/{zoneId}/soil/trends` is received with a valid Bearer token and optional parameter: days (Integer, default 30)<br>**When** the API computes soil trends over the specified period<br>**Then** the API responds with 200 OK returning: zoneId (Long), periodDays (Integer), moistureTrend (String), temperatureTrend (String), phTrend (String), soilHealthScore (Integer), recommendations (List), generatedAt (String). | EP-009 |
| **EP-009-MS012** | Integrate External Weather API to Enrich Agronomic Diagnosis | As a Cloud Developer, I want the Cloud API to integrate an external weather API so that the diagnosis engine can consider forecasted precipitation, temperature, and humidity when calculating water stress to prevent over-irrigation before forecasted rain. | **Scenario 1: Diagnosis is enriched with external weather forecast data**<br>**Given** that the rules engine evaluates irrigation conditions for a zone<br>**When** the external weather API returns data indicating imminent precipitation within the forecast window<br>**Then** the system adjusts the water stress index downward and includes the forecast information in the diagnosis output.<br><br>**Scenario 2: System gracefully degrades when the weather service is unavailable**<br>**Given** that the external weather service returns a timeout or server error<br>**When** the diagnosis engine requires climate data<br>**Then** the system proceeds using only internal soil telemetry, sets weatherContextAvailable to false, and logs the external service failure. | EP-009 |
| **EP-010** | Customer Support and IoT Fleet Monitoring | Provide support staff with tools to monitor device health fleet-wide, detect incidents, and resolve issues without field visits through Cloud API, Web App, and Embedded. | N/A (Es un Epic) | EP-010-US001, EP-010-TS029, EP-010-MS007 |
| **EP-010-US001** | Monitor IoT Device Health Fleet-Wide | As a support staff member, I want to monitor the health of the entire IoT device fleet so that I can proactively manage deployed devices, schedule maintenance, and resolve issues without unnecessary physical field visits. | **Scenario 1: Device health dashboard displays fleet status**<br>**Given** that the support staff member accesses the device fleet management module<br>**When** the fleet status is retrieved from the Cloud API<br>**Then** the system displays the connection, battery, and signal status for each deployed device, with the ability to filter by critical issues.<br><br>**Scenario 2: Maintenance work order is scheduled for devices with critically low battery**<br>**Given** that the system identifies devices with battery levels below the critical threshold<br>**When** the support staff member schedules a maintenance visit with a target date and assigns a technician<br>**Then** the system generates a work order, notifies the assigned technician, and dispatches a notification to the associated farmer with the scheduled date. | EP-010 |
| **EP-010-TS029** | Cloud RESTful API — Fleet Health Endpoints | As a Frontend Developer, I want the Cloud API to implement fleet health monitoring endpoints so that the support Web Application can retrieve and aggregate device connectivity, battery, and signal metrics across the entire IoT fleet. | **Scenario 1: Aggregated fleet status is retrieved**<br>**Given** a GET request to `/api/v1/admin/fleet/status` is received with a valid admin or support Bearer token<br>**When** the API queries active devices and aggregates health metrics<br>**Then** the API responds with 200 OK returning: totalDevices (Integer), onlineDevices (Integer), offlineDevices (Integer), errorDevices (Integer), lowBatteryDevices (Integer), devicesByType (Object), averageSignalStrength (Double), lastUpdated (String).<br><br>**Scenario 2: Fleet device list is retrieved with pagination and filters**<br>**Given** a GET request to `/api/v1/admin/devices/fleet` is received with a valid admin or support Bearer token and optional query parameters: status (String), batteryLevelBelow (Double), limit (Integer)<br>**When** the API retrieves the device list<br>**Then** the API responds with 200 OK returning a list with: deviceId (Long), serialNumber (String), deviceType (String), ownerName (String), status (String), batteryLevel (Double), signalStrength (Integer), lastSeenAt (String).<br><br>**Scenario 3: Unauthorized user is denied access**<br>**Given** a GET request to `/api/v1/admin/fleet/status` is received with a token from a non-admin and non-support user<br>**When** the API evaluates the role<br>**Then** the API responds with 403 Forbidden and logs the unauthorized attempt. | EP-010 |
| **EP-010-MS007** | Implement Remote Hardware Diagnostics | As a Cloud Developer, I want the Cloud API to support remote hardware diagnostics so that the support team can execute diagnostic tests on embedded sensors and valves directly from the cloud without dispatching a field technician. | **Scenario 1: Remote diagnostic session is initiated**<br>**Given** a POST request to `/api/v1/admin/devices/{deviceId}/diagnostics` is received with a valid admin or support Bearer token<br>**When** the API dispatches the diagnostic instruction to the target device via the Edge<br>**Then** the API responds with 202 Accepted returning: diagnosticId (Long), deviceId (Long), status (String: PENDING), createdAt (String).<br><br>**Scenario 2: Diagnostic report is retrieved with component-level results**<br>**Given** a GET request to `/api/v1/admin/devices/{deviceId}/diagnostics/{diagnosticId}` is received after diagnostic completion<br>**When** the API retrieves the results reported by the device<br>**Then** the API responds with 200 OK returning: diagnosticId (Long), deviceId (Long), status (String: COMPLETED), componentResults (List): [{ component (String), status (String: OK, FAULTY, DEGRADED), details (String) }], recommendation (String), completedAt (String). | EP-010 |
| **EP-011** | Operations, Admin and Customer Account Management | Enable the Ops & Billing team to manage customer lifecycle, device activations, account suspensions, and internal administrative workflows through Cloud API and Web App. | N/A (Es un Epic) | EP-011-US001, EP-011-US002, EP-011-US003, EP-011-TS030 |
| **EP-011-US001** | Manage Customer Accounts from Admin Panel | As an operations specialist, I want to manage customer accounts from the admin panel so that I can control the customer lifecycle, handle account suspensions for payment defaults, and reactivate accounts. | **Scenario 1: Customer account is searched and viewed**<br>**Given** that the operations specialist accesses the customer management module<br>**When** a specific customer is searched by name, email, or status<br>**Then** the system displays the full profile including the active plan, linked devices, payment history, and account status.<br><br>**Scenario 2: Account is suspended due to payment default**<br>**Given** that a customer has exceeded the payment grace period<br>**When** the operations specialist executes the suspension command with the reason<br>**Then** the system disables platform access, retains all historical data, dispatches a notification to the customer, and logs the administrative action.<br><br>**Scenario 3: Suspended account is reactivated after payment resolution**<br>**Given** that a previously suspended customer has settled their payment<br>**When** the operations specialist triggers the reactivation<br>**Then** the system restores full platform access, resumes device telemetry synchronization, and sends a confirmation to the customer. | EP-011 |
| **EP-011-US002** | Manage and Activate IoT Devices from Admin Panel | As an operations specialist, I want to manage and activate IoT devices from the admin panel so that new hardware batches can be registered efficiently and compromised devices can be deactivated. | **Scenario 1: New batch of devices is registered**<br>**Given** that the operations team receives a new batch of IoT hardware<br>**When** the specialist uploads a file containing serial numbers and device types or enters them manually<br>**Then** the system registers all devices and updates their status to AVAILABLE for farmer activation.<br><br>**Scenario 2: Device is deactivated after being reported as lost or stolen**<br>**Given** that a farmer or support staff reports a physical device as lost or stolen<br>**When** the operations specialist executes the deactivation with the reason<br>**Then** the system invalidates the device's security credentials, permanently blocks telemetry ingestion, and records the deactivation in the audit trail. | EP-011 |
| **EP-011-US003** | List and Manage Platform Users (Admin) | As an operations specialist, I want to list and search all platform users so that I can manage user accounts and their assigned roles from the admin panel. | **Scenario 1: User list is retrieved with search and filter options**<br>**Given** that the operations specialist accesses the user management module with optional search, role, and status filters<br>**When** the system retrieves matching users<br>**Then** the system displays the list with full name, email, role, account status, verification status, subscription plan, and creation date.<br><br>**Scenario 2: User roles are updated**<br>**Given** that the operations specialist modifies the roles of a specific user<br>**When** the system validates and updates the roles<br>**Then** the system applies the new role configuration, invalidates any affected active sessions, and confirms the update. | EP-011 |
| **EP-011-TS030** | Cloud RESTful API — Administrative Operations Endpoints | As a Frontend Developer, I want the Cloud API to implement administrative endpoints so that the back-office Web Application can securely manage customer accounts, device registries, and account suspensions. | **Scenario 1: Batch of IoT devices is registered**<br>**Given** a POST request to `/api/v1/admin/devices/batch` is received with a valid admin Bearer token and an array of device objects: [{ serialNumber (String), deviceType (String), manufacturer (String), model (String) }]<br>**When** the API validates admin privileges, parses serial numbers, and persists registries<br>**Then** the API responds with 201 Created returning a summary: registered (Integer), failed (Integer), errors (List, nullable).<br><br>**Scenario 2: User account is suspended**<br>**Given** a POST request to `/api/v1/admin/users/{userId}/suspend` is received with a valid admin Bearer token and attribute: reason (String)<br>**When** the API processes the suspension<br>**Then** the API responds with 200 OK, updates account status to SUSPENDED, invalidates all active JWT sessions, and queues a suspension notification.<br><br>**Scenario 3: Suspended account is reactivated**<br>**Given** a POST request to `/api/v1/admin/users/{userId}/reactivate` is received with a valid admin Bearer token<br>**When** the API processes the reactivation<br>**Then** the API responds with 200 OK, restores full platform access, resumes device telemetry sync, and notifies the customer.<br><br>**Scenario 4: Unauthorized admin access attempt is rejected**<br>**Given** any request to `/api/v1/admin/*` is received with a token from a non-admin user<br>**When** the API evaluates the role<br>**Then** the API responds with 403 Forbidden and logs the unauthorized access event. | EP-011 |
| **EP-012** | Business Intelligence and Strategic Analytics | Provide strategic dashboards and feature adoption metrics to allow the Product Owner to make data-driven decisions through Cloud API and Web App. | N/A (Es un Epic) | EP-012-US022, EP-012-US023, EP-012-US024, EP-012-US025, EP-012-US026, EP-012-TS031 |
| **EP-012-US022** | View Business Strategic Analytics | As a product owner, I want to review growth, retention, and churn metrics segmented by customer type so that I can adjust the quarterly product roadmap and strategies based on data-driven insights. | **Scenario 1: Executive dashboard displays KPIs with period comparison**<br>**Given** that the product owner accesses the executive analytics dashboard<br>**When** the system retrieves aggregated metrics for the selected period<br>**Then** the system displays active users, MRR, conversion rate, and churn rate with trend indicators comparing the current period to the previous one.<br><br>**Scenario 2: Churn is analyzed by customer segment**<br>**Given** that the product owner analyzes cancellation reasons segmented by farmer and agronomist<br>**When** the segment filter is applied<br>**Then** the system displays churn statistics by customer type and the most frequently reported cancellation reasons. | EP-012 |
| **EP-012-US023** | View Feature Adoption and Usage Heatmap | As a product owner, I want to visualize which features are used most and least frequently by each user segment so that I can prioritize roadmap improvements based on real usage data. | **Scenario 1: Feature usage is displayed by segment and period**<br>**Given** that the product owner accesses the product analytics module and selects a target segment and time period<br>**When** the system retrieves the feature usage data<br>**Then** the system displays the usage frequency for each platform feature, identifying the most and least adopted by the selected segment.<br><br>**Scenario 2: Feature funnel analysis identifies high-abandonment steps**<br>**Given** that the product owner analyzes the user flow funnel for a specific feature<br>**When** the system retrieves step-by-step completion data<br>**Then** the system identifies the funnel step with the highest abandonment rate and the completion rate for successful users. | EP-012 |
| **EP-012-US024** | Get Farmer Dashboard Summary | As a Frontend Developer, I want the Cloud API to provide an endpoint that aggregates the farmer's key metrics across all farms so that I can render the main farmer dashboard. | **Scenario 1: Farmer dashboard summary is retrieved**<br>**Given** a GET request to `/api/v1/dashboard/farmer` is received with a valid Bearer token for a farmer user<br>**When** the API aggregates the farmer's metrics across all farms<br>**Then** the API responds with 200 OK returning: userId (Long), farmCount (Integer), zoneCount (Integer), deviceCount (Integer), onlineDevices (Integer), offlineDevices (Integer), activeIrrigations (Integer), unreadNotifications (Integer), activeAlerts (Integer), recentSecurityEvents (Integer), soilHealthAverage (Integer), lastUpdated (String).<br><br>**Scenario 2: Non-farmer access is denied**<br>**Given** a GET request to `/api/v1/dashboard/farmer` is received with a token from a non-farmer<br>**When** the API checks the role<br>**Then** the API responds with 403 Forbidden. | EP-012 |
| **EP-012-US025** | Get Agronomist Dashboard Summary | As a Frontend Developer, I want the Cloud API to provide an endpoint that aggregates the agronomist's key metrics across all clients so that I can render the main agronomist dashboard. | **Scenario 1: Agronomist dashboard summary is retrieved**<br>**Given** a GET request to `/api/v1/dashboard/agronomist` is received with a valid Bearer token for an agronomist user<br>**When** the API aggregates the agronomist's metrics across all clients<br>**Then** the API responds with 200 OK returning: userId (Long), totalClients (Integer), activeClients (Integer), pendingClients (Integer), totalFarmsMonitored (Integer), openRecommendations (Integer), urgentRecommendations (Integer), unreadNotifications (Integer), lastUpdated (String).<br><br>**Scenario 2: Non-agronomist access is denied**<br>**Given** a GET request to `/api/v1/dashboard/agronomist` is received with a token from a non-agronomist<br>**When** the API checks the role<br>**Then** the API responds with 403 Forbidden. | EP-012 |
| **EP-012-US026** | Get Priority Cases and Alerts | As a Frontend Developer, I want the Cloud API to provide an endpoint that compiles and prioritizes the most critical cases for the authenticated user so that the dashboard can display actionable issues ordered by severity. | **Scenario 1: Priority cases are retrieved ordered by severity**<br>**Given** a GET request to `/api/v1/dashboard/priority-cases` is received with a valid Bearer token and optional parameter: limit (Integer, default 10)<br>**When** the API compiles and ranks active alerts and pending actions for the user's scope<br>**Then** the API responds with 200 OK returning a list ordered by severity where each item contains: caseId (String), caseType (String), severity (String), farmId (Long), farmName (String), zoneId (Long, nullable), description (String), detectedAt (String), acknowledged (Boolean).<br><br>**Scenario 2: No active priority cases exist**<br>**Given** a GET request to `/api/v1/dashboard/priority-cases` is received for a user with no active cases<br>**When** the API finds no cases<br>**Then** the API responds with 200 OK returning an empty list. | EP-012 |
| **EP-012-TS031** | Cloud RESTful API — Business Intelligence Endpoints | As a Frontend Developer, I want the Cloud API to implement business intelligence and analytical endpoints so that the BI Web Application can retrieve aggregated usage metrics, financial KPIs, and feature adoption data. | **Scenario 1: Executive dashboard KPIs are aggregated and returned**<br>**Given** a GET request to `/api/v1/admin/analytics/executive` is received with a valid admin Bearer token and optional parameters: period (String), from (String), to (String)<br>**When** the API queries operational and billing databases, aggregates data, and computes metrics<br>**Then** the API responds with 200 OK returning: activeUsers (Integer), mrr (Double), currency (String), conversionRate (Double), churnRate (Double), newSubscriptions (Integer), canceledSubscriptions (Integer), periodComparison (Object).<br><br>**Scenario 2: Churn analysis is broken down by customer segment**<br>**Given** a GET request to `/api/v1/admin/analytics/churn` is received with a valid admin Bearer token and optional parameter: segment (String: FARMER, AGRONOMIST)<br>**When** the API aggregates churn metrics by segment<br>**Then** the API responds with 200 OK returning churn statistics by customer type and most frequent cancellation reasons.<br><br>**Scenario 3: Unauthorized user is denied access to BI endpoints**<br>**Given** a GET request to `/api/v1/admin/analytics/*` is received with a token from a non-admin user<br>**When** the API evaluates the role<br>**Then** the API responds with 403 Forbidden and logs the security event. | EP-012 |
| **EP-012-US027** | Get Authenticated User Profile | As a Frontend Developer, I want the Cloud API to provide an endpoint to retrieve the currently authenticated user's profile so that I can display user information in the application header, settings, and personalization features across all applications. | **Scenario 1: Authenticated user profile is retrieved**<br>**Given** a GET request to `/api/v1/me` is received with a valid Bearer token<br>**When** the API resolves the user identity from the JWT token<br>**Then** the API responds with 200 OK returning: id (Long), fullName (String), email (String), role (String), verified (Boolean), createdAt (String).<br><br>**Scenario 2: Unauthenticated request is rejected**<br>**Given** a GET request to `/api/v1/me` is received without a valid Bearer token<br>**When** the API cannot authenticate the request<br>**Then** the API responds with 401 Unauthorized. | EP-012 |

## 3.2. Impact Mapping

En esta sección, presentamos el **Impact Mapping** el cual como finalidad alinear los objetivos de negocio que tiene SATECHO, bajo el criterio **SMART** (Specific, Measurable, Attainable, Relevant y Timely), con las necesidades y comportamientos esperados de cada uno de nuestros **User Personas**. A través de este actividad, identificamos cómo los actores clave (Agricultor e Ingeniero Agrónomo) deben cambiar su comportamiento (Impacts) para ayudarnos a alcanzar las metas, qué funcionalidades o artefactos construiremos (Deliverables) para provocar esos cambios, y cómo se traducen estos en historias de usuario (User Stories) concretas para el equipo de desarrollo.

## Agricultor

El Impact Mapping asociado a Danilo Subasto representa la alineación estratégica entre los objetivos de negocio planteados para la solución digital orientada al sector agrícola, las necesidades reales del usuario final y los entregables funcionales del producto de software que permitirán generar valor medible. Este mapa de impacto permite visualizar cómo cada iniciativa tecnológica responde directamente a los retos operativos del agricultor, asegurando que el desarrollo de la plataforma no se enfoque únicamente en funcionalidades aisladas, sino en resultados concretos que impulsen la adopción, eficiencia operativa y seguridad del usuario.

![Impact Mapping - Agricultor](./assets/images/impact-mapping/impact-mapping-agricultor.png)
![Impact Mapping - Agricultor](./assets/images/impact-mapping/impact-mapping-agricultor.png)

En primer lugar, se establece como objetivo comercial la adquisición de 50 agricultores suscritos al plan Premium durante los primeros seis meses de operación. Para alcanzar esta meta, se identifica que Danilo Subasto necesita confiar en la solución antes de realizar una inversión económica, por lo que debe percibir beneficios claros, casos reales de éxito y una propuesta de valor transparente. En respuesta a ello, el producto contempla entregables clave como una landing page informativa, donde se explique el servicio de manera clara y se presenten testimonios verificables de otros productores; un formulario rápido para solicitar pilotos gratuitos, que reduzca barreras de entrada y facilite la prueba del sistema; y una página de precios estructurada, donde se comparen los planes Básico y Premium de forma sencilla. Estas funcionalidades buscan convertir el interés inicial en registros efectivos y futuras suscripciones pagadas.

Como segundo eje estratégico, se plantea lograr que el 80% de los usuarios activos reduzcan su consumo de agua en al menos un 20% durante su primer ciclo de cultivo, promoviendo eficiencia hídrica y sostenibilidad productiva. Desde la perspectiva del usuario, Danilo necesita conocer con precisión el estado de humedad de su terreno para dejar de regar por intuición o por costumbre. Para ello, la solución propone un dashboard web y móvil en tiempo real, con una interfaz visual tipo semáforo (verde, amarillo y rojo) que permita interpretar rápidamente la condición del suelo sin requerir conocimientos técnicos avanzados. Complementariamente, se integra un sistema de alertas automáticas por SMS o WhatsApp que notifique episodios de estrés hídrico y un módulo de control remoto para electroválvulas, permitiendo activar o detener el riego desde la aplicación. Estas capacidades tecnológicas facilitan decisiones oportunas, ahorro de recursos y automatización operativa.

Finalmente, el tercer objetivo de negocio consiste en reducir en un 40% los reportes de incidentes de seguridad y falsas alarmas durante el primer año. En este contexto, Danilo requiere disminuir la necesidad de vigilancia nocturna presencial y contar con mecanismos inteligentes que filtren eventos irrelevantes. Para responder a esta necesidad, se plantea un módulo de seguridad con clasificación de amenazas mediante Inteligencia Artificial, capaz de diferenciar entre personas, animales u otros movimientos no críticos, reduciendo alertas innecesarias. Asimismo, se incorpora una configuración de Modo Vigilancia Nocturna, mediante la cual el usuario puede programar horarios específicos para activar monitoreo reforzado únicamente en franjas horarias sensibles. Estas funcionalidades incrementan la tranquilidad del agricultor, optimizan tiempos de supervisión y fortalecen la protección de la infraestructura agrícola.

## Ingeniero Agrónomo

El Impact Mapping asociado a Nestor Campo representa la alineación estratégica entre nuestros objetivos de negocio y las necesidades reales de los ingenieros agrónomos independientes que interactuarán con el sistema. Este mapa visualiza de manera estructurada cómo el comportamiento de nuestro usuario clave impacta directamente en el éxito de la plataforma, y detalla qué entregables de software son necesarios para facilitar dicho comportamiento, garantizando que todo el esfuerzo de desarrollo esté justificado por el valor que aporta.

![Impact Mapping - Agronomo](./assets/images/impact-mapping/impact-mapping-agronomo.png)
![Impact Mapping - Agronomo](./assets/images/impact-mapping/impact-mapping-agronomo.png)

En cuanto al primer eje estratégico, el cual se titula **Adquisición y Adopción Inicial de la Plataforma**, la estrategia se centra en la transición del agrónomo desde métodos tradicionales hacia un ecosistema digital centralizado. Para alcanzar la meta de 20 profesionales registrados y la vinculación efectiva de clientes en el primer semestre, el producto despliega una Landing Page B2B diseñada específicamente para asesores técnicos y cooperativas, actuando como el primer punto de contacto donde se comunica el valor de la escalabilidad operativa. Este proceso se consolida mediante un portal de Onboarding seguro que no solo gestiona la identidad del usuario, sino que le otorga la capacidad técnica de administrar múltiples perfiles de clientes desde una única cuenta centralizada, reduciendo significativamente la fricción de entrada y los costos de desplazamiento físico desde el primer día.

Respecto al segundo eje llamado **Eficiencia Operativa y Diagnóstico Remoto**, la plataforma busca escalar la capacidad de gestión del ingeniero en un 50% mediante la implementación de herramientas de monitoreo de alta precisión. El núcleo técnico de esta solución reside en un Dashboard web centralizado con vista multi-cliente, el cual permite la supervisión simultánea de diversas parcelas e identifica anomalías críticas mediante un sistema de alertas visuales. Este diagnóstico se sustenta en un módulo de gráficos históricos avanzados que procesan variables críticas como humedad, conductividad eléctrica (EC) y pH, permitiendo un análisis científico de tendencias. Al integrar paneles de configuración de umbrales personalizados, el sistema automatiza la detección de estrés hídrico o salinidad, notificando al usuario en tiempo real cuando los niveles se desvían del rango óptimo, lo que transforma la consultoría presencial en una gestión basada en datos y respuesta inmediata.

Finalmente, como úlimo eje asociado al ámbito de la **Automatización de Tareas y Profesionalización del Servicio**; el enfoque se dirige a la sustitución de registros manuales y hojas de cálculo por flujos de trabajo automatizados que aseguren un cumplimiento del 90% en la entrega de informes técnicos. La plataforma integra un generador automático de reportes PDF que sintetiza los datos agronómicos recolectados durante la semana en un entregable profesional listo para ser distribuido a través de canales de mensajería instantánea. Como complemento crítico para la gobernanza del riego, se incluye un registro de actividad (log) que monitorea el estado de las electroválvulas. Esta funcionalidad permite al agrónomo realizar una auditoría técnica sobre la ejecución de sus recomendaciones en el campo, verificando tiempos exactos de activación de bombas de agua y garantizando que el agricultor cumpla rigurosamente con las instrucciones de riego proporcionadas, elevando así el estándar de calidad del servicio de asesoría.

## 3.3. Product Backlog

En esta sección presentamos el motor estratégico de Satecho: nuestro **Product Backlog**. Más que un inventario de requerimientos gestionado a través de Jira, concebimos este espacio como una hoja de ruta viva y enfocada en la entrega de valor continuo. Para asegurar una planificación transparente y realista, hemos dimensionado cada historia de usuario utilizando la escala de Fibonacci (1, 2, 3, 5, 8). Esta técnica ágil nos permite estimar el esfuerzo y la complejidad de forma relativa, facilitando que tanto el equipo de desarrollo como los tomadores de decisiones compartan una misma perspectiva sobre lo que implica cada reto. En consecuencia, la estructura de nuestro backlog no es aleatoria; está rigurosamente priorizada de forma descendente.

En la cúspide se posicionan las iniciativas de mayor impacto y criticidad para los objetivos del negocio, garantizando que nuestros recursos se dirijan siempre a construir primero lo que Satecho necesita con mayor urgencia.

![Product Backlog-Satecho](./assets/images/product-backlog/product-backlog-satecho.png)

Podrá observar el *Product Backlog*, desarrollado en Jira en el enlace que se presenta a continuación: [Product Backlog-Satecho](https://satecho.atlassian.net/issues?jql=)

El _Product Backlog_ detallado lo puede observa en la siguiente tabla:

| # Orden | User Story Id | Título | Descripción | Story Points (1 / 2 / 3 / 5 / 8) |
| :--- | :--- | :--- | :--- | :--- |
| **1** | EP-001-US001 | Browse Landing Page Content | As a visitor, I want to browse landing page content so that I can understand the product's value proposition and available plans. | 2 |
| **2** | EP-001-US005 | Browse Landing as Agronomist Visitor | As an agronomist visitor, I want to browse the landing page so that I can see the benefits tailored to consultants. | 1 |
| **3** | EP-001-US006 | Watch Product Demo Video | As a visitor, I want to watch the product demo video so that I can understand real-world use cases before signing up. | 1 |
| **4** | EP-001-US007 | Request Commercial Demo | As a visitor, I want to request a commercial demo so that a representative can contact me to evaluate the solution. | 2 |
| **5** | EP-001-US002 | Register as Lead | As a visitor, I want to register as a lead so that I can create my account and start the onboarding process. | 3 |
| **6** | EP-001-US003 | Verify Email Account | As a newly registered user, I want to verify my email account so that I can confirm my identity and activate my access. | 3 |
| **7** | EP-001-US004 | Complete Onboarding Wizard | As a newly registered user, I want to complete the onboarding wizard so that I can configure my plot data, irrigation zones, and get guided into the platform from day one. | 5 |
| **8** | EP-002-US001 | View Real-Time Soil Dashboard | As an agriculturist, I want to view the real-time soil dashboard so that I can monitor moisture, EC, pH, and temperature of my parcels at any time. | 8 |
| **9** | EP-002-US012 | View EC and pH Trends | As an agriculturist, I want to view EC and pH trends so that I can analyze soil chemistry over time and detect salinity issues before they affect my crops. | 5 |
| **10** | EP-002-US002 | Receive Irrigation Alert | As an agriculturist, I want to receive irrigation alerts so that I am notified immediately when the soil reaches critical conditions and I can take action. | 5 |
| **11** | EP-002-US003 | Activate Irrigation Remotely | As an agriculturist, I want to activate irrigation remotely so that I can start watering my field from anywhere without being physically present. | 8 |
| **12** | EP-002-US004 | Stop Irrigation Manually | As an agriculturist, I want to stop irrigation manually so that I can interrupt watering early and avoid water waste when conditions change. | 3 |
| **13** | EP-002-TS007 | Embedded ESP32 Reads Sensors | As a Developer, I want the firmware to periodically read sensors and transmit data to the Edge so that a continuous flow of soil data is guaranteed. | 5 |
| **14** | EP-005-TS004 | Edge API Receives and Validates Sensor Telemetry | As a Developer, I want the Edge component to receive and store telemetry locally so that data reaches the system even with intermittent connectivity. | 8 |
| **15** | EP-002-TS005 | Cloud RESTful API - Telemetry Ingestion | As a Developer, I want the backend to implement a telemetry ingestion endpoint so that sensor data is securely received and stored. | 5 |
| **16** | EP-002-TS015 | Cloud RESTful API - Irrigation Commands | As a Developer, I want the backend to implement irrigation command endpoints so that the frontend can send valve orders with full traceability. | 5 |
| **17** | EP-002-TS006 | Edge API Controls Irrigation Valve | As a Developer, I want the Edge component to process valve commands from the server so that irrigation is executed accurately. | 5 |
| **18** | EP-002-TS009 | Embedded ESP32 Controls Valve | As a Developer, I want the firmware to receive valve commands and execute them reliably so that irrigation is activated and stopped correctly. | 8 |
| **19** | EP-004-US008 | Use App in Offline Mode | As an agriculturist, I want to use the app in offline mode so that I can view cached sensor data even without an internet connection in the field. | 5 |
| **20** | EP-004-TS025 | Implement Offline Command Queue | As a Developer, I want to manage an offline command queue in the mobile app so that requested irrigation operations are executed automatically upon reconnection. | 8 |
| **21** | EP-004-US009 | Receive and Interact with Push Notifications Natively | As an agriculturist, I want to receive and interact with push notifications natively so that I can be alerted of critical conditions directly from my device's notification center. | 3 |
| **22** | EP-004-TS023 | Configure Firebase Cloud Messaging for Push Notifications | As a Developer, I want to integrate FCM in the mobile app so that farmers receive reliable push notifications on Android even with the app closed. | 5 |
| **23** | EP-004-TS024 | Implement Deep Linking from Notification to Valve Control Screen | As a Developer, I want to implement deep linking from notifications directly to the valve control screen so that the farmer can navigate to the exact plot with a single tap. | 5 |
| **24** | EP-004-US010 | Open Irrigation Valve from Alert | As an agriculturist, I want to open the irrigation valve directly from an alert so that I can activate watering immediately without having to manually search for the parcel in the app. | 3 |
| **25** | EP-003-SS001 | Research Thermal PIR vs RGB Camera | As a Developer, I want to research and compare the accuracy and energy consumption of a thermal PIR sensor vs an RGB camera so that the best hardware for the security MVP can be defined. | 5 |
| **26** | EP-003-SS002 | Research ML Model Size for Edge Classification | As a Developer, I want to research the maximum classification model size executable on the ESP32 so that the AI architecture can be defined. | 5 |
| **27** | EP-003-TS010 | Embedded ESP32 Processes PIR Motion Detection | As a Developer, I want the firmware to detect motion and capture raw data so that the Edge can classify the threat. | 5 |
| **28** | EP-003-TS006 | Edge API Processes PIR Classification Locally | As a Developer, I want the Edge to run the classification algorithm locally so that latency is reduced and offline operation is guaranteed. | 8 |
| **29** | EP-003-TS011 | Cloud RESTful API — Perimeter Security Endpoints | As a Developer, I want the backend to implement RESTful endpoints for perimeter security so that clients can query intrusion events. | 3 |
| **30** | EP-003-US005 | View Perimeter Security Events | As an agriculturist, I want to view perimeter security events so that I can review a classified log of intrusion attempts and filter by event type. | 5 |
| **31** | EP-003-US006 | Receive Perimeter Intrusion Alert | As an agriculturist, I want to receive perimeter intrusion alerts so that I can respond to human detections in real-time. | 3 |
| **32** | EP-003-US007 | Configure Perimeter Security Settings | As an agriculturist, I want to configure perimeter security settings so that I can adjust detection sensitivity and define active monitoring schedules. | 5 |
| **33** | EP-009-US001 | View Multi-Parcel Dashboard | As an agronomist, I want to view a multi-parcel dashboard so that I can monitor all my clients' parcels from a single consolidated view. | 8 |
| **34** | EP-009-TS016 | Cloud RESTful API — Agronomic Diagnosis Engine | As a Developer, I want to implement an agronomic diagnosis endpoint combining soil data with weather context so that automatic recommendations are generated. | 8 |
| **35** | EP-009-MS012 | Integrate External Weather API to Enrich Diagnosis | As a Developer, I want to integrate an external weather API so that the diagnosis engine considers climate conditions when calculating water stress. | 5 |
| **36** | EP-009-US004 | Monitor Remote Parcels Between Visits | As an agronomist, I want to monitor remote parcels between visits so that I can dispatch proactive recommendations upon critical environmental changes. | 3 |
| **37** | EP-009-US002 | Adjust Client Irrigation Thresholds Remotely | As an agronomist, I want to adjust client irrigation thresholds remotely so that I can update crop recommendations without a physical field visit. | 5 |
| **38** | EP-009-US003 | Generate Technical Report for Client | As an agronomist, I want to generate a technical report so that I can share monthly soil health summaries and recommendations in a professional format. | 5 |
| **39** | EP-009-US005 | Configure Crop-Specific Thresholds as Agronomist Template | As an agronomist, I want to configure crop-specific thresholds as templates so that I can quickly apply standardized settings to new client parcels. | 3 |
| **40** | EP-008-US020 | Manage Advanced Notification Preferences | As an agriculturist, I want to manage advanced notification preferences so that I can route alerts only to the appropriate channels. | 3 |
| **41** | EP-008-US021 | Receive WhatsApp Alert with Direct Action Link | As an agriculturist, I want to receive WhatsApp alerts with direct action links so that I can address critical conditions immediately from my messaging app. | 5 |
| **42** | EP-008-TS018 | Cloud RESTful API — Notifications Dispatch | As a Developer, I want to implement a multi-channel notification dispatch service in the backend so that internal modules can queue alerts for external delivery. | 8 |
| **43** | EP-006-US014 | View and Manage Subscription Plan | As an agriculturist, I want to view and manage my subscription plan so that I can evaluate the current tier, benefits, and upgrade options. | 3 |
| **44** | EP-006-US015 | Manage Billing and Payment Methods | As an agriculturist, I want to manage billing and payment methods so that I can access my payment history and update my credit cards. | 3 |
| **45** | EP-006-US016 | Cancel Subscription and Request Hardware Return | As an agriculturist, I want to cancel my subscription and request a hardware return so that I can handle the offboarding process securely. | 3 |
| **46** | EP-006-TS017 | Cloud RESTful API — Subscriptions & Billing | As a Developer, I want to implement subscription endpoints integrated with an external payment provider in the backend so that plans are managed automatically. | 8 |
| **47** | EP-010-US001 | Monitor IoT Device Health Fleet | As a support staff member, I want to monitor the IoT device health fleet so that I can manage and maintain deployed devices proactively without unnecessary field visits. | 5 |
| **48** | EP-010-TS029 | Cloud RESTful API — Fleet Health Endpoints | As a Developer, I want to implement fleet health endpoints in the backend so that the support SPA can retrieve and aggregate connection and battery statuses securely. | 5 |
| **49** | EP-011-US001 | Manage Customer Accounts from Admin Panel | As an operations specialist, I want to manage customer accounts from the admin panel so that the customer lifecycle and account suspensions can be controlled. | 5 |
| **50** | EP-011-US002 | Manage and Activate IoT Devices from Admin Panel | As an operations specialist, I want to manage and activate IoT devices from the admin panel so that new hardware batches are registered and compromised devices are blocked. | 5 |
| **51** | EP-011-TS030 | Cloud RESTful API — Admin Endpoints | As a Developer, I want to implement administrative endpoints in the backend so that the back-office SPA can securely manage accounts and device registries. | 5 |
| **52** | EP-012-US022 | View Business Strategic Analytics | As a product owner, I want to review growth, retention, and churn metrics by segment so that the quarterly roadmap and strategies can be adjusted. | 5 |
| **53** | EP-012-US023 | View Feature Adoption and Usage Heatmap | As a product owner, I want to see which features are used and how frequently by segment so that roadmap improvements can be prioritized based on real usage. | 5 |
| **54** | EP-012-TS031 | Cloud RESTful API — Business Intelligence Endpoints | As a Developer, I want to implement analytical endpoints in the backend so that the BI SPA can retrieve aggregated usage and financial metrics. | 8 |
| **55** | EP-007-TS021 | Implement Lazy Loading and Code Splitting for Dashboard Modules | As a Developer, I want the system to implement lazy loading of dashboard modules so that the initial load time is reduced. | 5 |
| **56** | EP-007-TS022 | Implement Global State Management for Dashboard | As a Developer, I want to implement a global state manager on the web frontend so that sensor data, alerts, and configuration are synchronized between components without redundant API calls. | 5 |
| **57** | EP-007-TS027 | Internationalization Support | As a Developer, I want the system to support internationalization so that the platform can be used comfortably without language barriers. | 3 |
| **58** | EP-007-TS028 | Accessibility Compliance | As a Developer, I want the system to comply with accessibility standards so that users with disabilities can access all features without barriers. | 3 |
| **59** | EP-005-MS001 | Implement Async Event Processing Queue for IoT Telemetry | As a Developer, I want the system to implement an asynchronous message queue so that ingestion is decoupled from rule processing and data loss during load spikes is avoided. | 8 |
| **60** | EP-005-MS006 | Implement Over-The-Air Firmware Update (OTA) | As a Developer, I want the system to support remote firmware updates so that improvements and security fixes are distributed without requiring field visits. | 8 |
| **61** | EP-007-MS002 | Implement Auto-Scaling for Backend Services | As a Developer, I want to configure horizontal auto-scaling for backend services so that performance is maintained during peak loads without unnecessary costs during low-demand periods. | 8 |
| **62** | EP-007-MS003 | Implement Data Retention and Archiving Policy | As a Developer, I want to implement a historical data retention and archiving policy so that storage costs are balanced with data availability for analysis. | 5 |
| **63** | EP-007-MS004 | Implement Automated Backup and Recovery | As a Developer, I want to configure automatic backups of the main database so that data can be recovered after a catastrophic failure with minimal data loss. | 8 |
| **64** | EP-007-MS014 | Setup CI/CD Pipeline for Modular Backend | As a Developer, I want to configure a CI/CD pipeline that compiles, tests, and deploys backend modules so that release time and deployment risk are reduced. | 8 |
| **65** | EP-005-MS008 | Implement Edge-to-Cloud Sync Protocol | As a Developer, I want to implement the Edge-to-Cloud synchronization protocol with delivery guarantee so that no data is lost during intermittent connectivity. | 8 |
| **66** | EP-005-MS009 | Implement Edge API Authentication for IoT Devices | As a Developer, I want to implement mutual TLS authentication between ESP32 devices and the Edge component so that only authorized devices can communicate. | 8 |
| **67** | EP-005-MS010 | Implement Local Rule Engine on Edge | As a Developer, I want the Edge to evaluate threshold rules locally so that critical actions are guaranteed even without cloud connectivity. | 8 |
| **68** | EP-005-MS011 | Implement Edge Heartbeat and Health Reporting | As a Developer, I want the Edge to transmit periodic heartbeats so that offline nodes are detected automatically. | 5 |
| **69** | EP-005-MS013 | Implement IoT Gateway Auto-Reconnection Protocol | As a Developer, I want to implement an automatic reconnection protocol with a command queue so that solenoid valve activation is guaranteed even with network intermittency. | 8 |
| **70** | EP-010-MS007 | Implement Remote Hardware Diagnostics | As a Developer, I want to implement remote hardware diagnostics so that the support system can execute tests on the embedded sensors and valves directly from the cloud. | 8 |
| **71** | EP-002-TS026 | Implement Biometric Auth for Critical Actions | As a Developer, I want the mobile app to require biometric authentication for critical actions so that accidental valve activations are prevented. | 5 |
| **72** | EP-004-MS005 | Implement Zero-Touch Device Provisioning | As a Developer, I want the backend to support zero-touch provisioning so that hardware activation via QR scanning is secure and automated. | 8 |